# خطة معمارية: نظام صلاحيات متعدد المستخدمين (Owner / Cashier)

> وثيقة تصميم فقط — لا يوجد أي تنفيذ برمجي مرفق بها. التنفيذ يبدأ فقط بعد أمر صريح.

---

## 1. المشكلة الحالية (Current State)

بعد فحص الكود الفعلي في المشروع:

- **تسجيل الدخول حالياً مستخدم واحد لكل مطعم فقط.** `src/app/login/page.tsx` يحوّل اسم المستخدم إلى إيميل داخلي `slug@dasha.app`، ويربط `restaurants.owner_id` بحساب Supabase Auth واحد (`supabase_migration_multi_tenant.sql`).
- **لا يوجد أي مفهوم "دور" (role) في قاعدة البيانات حالياً.** كل من يملك اسم المستخدم وكلمة المرور يدخل على **كل** الداشبورد بدون استثناء: الطلبات، المخزون، الإحصائيات، الإعدادات، تغيير كلمة المرور، حذف الطلبات (`src/app/admin/dashboard/page.tsx:587` فيه `supabase.from('orders').delete()` بدون أي قيد صلاحية أو سجل تدقيق).
- **بيانات حساسة مكشوفة بالكامل لأي شخص يملك بيانات الدخول:** سعر التكلفة في المخزون (`التكلفة / وحدة` في `admin/inventory`)، الأرباح والتقارير في `admin/statistics`، وتغيير كلمة مرور الحساب نفسه من `admin/settings`.
- النتيجة: **لا يوجد فصل فعلي بين "صاحب المطعم" و"الكاشير"** — كلاهما نفس الحساب، نفس الصلاحية، ونفس الشاشة. هذا هو جوهر المشكلة التي نريد حلها.

---

## 2. الأدوار (Roles) — بالمعايير العالمية لأنظمة POS/SaaS

اعتماداً على ما تعتمده أنظمة عالمية (Toast POS، Square، Lightspeed، Odoo POS):

### 🟣 المالك / المدير (Owner / Manager)
المسؤول عن الاستراتيجية، المال، والمخزون. رؤية كاملة (God View) + تحكم كامل.

### 🟢 الكاشير (Cashier)
واجهة تشغيلية سريعة فقط: استقبال الطلب → الدفع → التسليم. لا رؤية مالية استراتيجية، ولا صلاحية تعديل بنية النظام.

> ملاحظة تصميمية مهمة: أنظمة عالمية ناجحة (Toast, Square) تضيف طبقة ثالثة اختيارية هي **"مدير الوردية / Shift Supervisor"** — صلاحياته بين الكاشير والمالك (يقدر يوافق على استرجاع أو خصم لكن ما يشوف الأرباح والتكلفة). أقترح نبنيها **من اليوم الأول كـ role قابل للتوسعة (enum قابل للامتداد)** حتى لو ما فعّلناها فعلياً الآن، لتفادي إعادة هيكلة لاحقاً.

---

## 3. مصفوفة الصلاحيات (Permission Matrix)

| الوحدة / الإجراء | المالك (Owner) | الكاشير (Cashier) |
|---|:---:|:---:|
| **الطلبات — استقبال/قبول/تجهيز** | ✅ | ✅ |
| **الطلبات — تعديل صنف قبل التأكيد** | ✅ | ✅ |
| **الطلبات — حذف/إلغاء طلب مؤكَّد** | ✅ مباشر | ⚠️ يتطلب **PIN موافقة مدير** + سبب إلزامي |
| **الطلبات — استرجاع مبلغ (Refund)** | ✅ مباشر | ⚠️ يتطلب **PIN موافقة مدير** |
| **تطبيق خصم على الطلب** | ✅ حر | ⚠️ ضمن حد أقصى معرَّف مسبقاً (مثلاً 10%)، وأي تجاوز يتطلب PIN |
| **الدفع — تحصيل نقدي/شبكة** | ✅ | ✅ |
| **فتح/إغلاق الوردية (Cash Drawer Session)** | ✅ | ✅ (لكن لا يقدر يتجاوز فرق التسوية بدون تسجيل) |
| **المخزون — عرض الكميات المتوفرة** | ✅ | ✅ (عرض فقط: متوفر/منتهي) |
| **المخزون — عرض سعر التكلفة (Cost Price)** | ✅ | ❌ **مخفي تماماً** |
| **المخزون — إضافة/تعديل/حذف مواد** | ✅ | ❌ |
| **القائمة (Menu) — تعديل الأصناف والأسعار** | ✅ | ❌ |
| **القائمة — تفعيل/تعطيل صنف "نفد" (86'd item)** | ✅ | ✅ (تبديل حالة سريع فقط) |
| **التقارير المالية والأرباح (Statistics)** | ✅ كامل | ❌ محجوب بالكامل |
| **تقرير الوردية الخاص به فقط (مبيعاته، عدد طلباته)** | ✅ | ✅ (تقرير شخصي فقط) |
| **إدارة الفروع** | ✅ | ❌ |
| **إدارة السائقين (Drivers)** | ✅ | 👁️ عرض حالة السائق المرتبط بطلبه فقط |
| **الإعدادات العامة للمطعم** | ✅ | ❌ |
| **إدارة المستخدمين (إضافة/حذف كاشير، صلاحيات)** | ✅ | ❌ |
| **سجل التدقيق (Audit Log)** | ✅ يرى كل شيء | 👁️ يرى سجل نفسه فقط (اختياري) |
| **تغيير كلمة مرور حسابه** | ✅ | ✅ (لحسابه فقط، لا يقدر يغيّر حساب المالك) |
| **الأرشيف (طلبات قديمة)** | ✅ كامل مع كل التفاصيل المالية | ✅ بدون تكلفة/ربح |

**المبدأ الحاكم:** الكاشير يرى ويتحكم فقط بما يلزمه لإتمام عملية بيع واحدة بسرعة. أي إجراء "غير قابل للرجوع" أو "يُنقص من الإيراد المسجّل" (حذف، استرجاع، خصم كبير) يمرّ ببوابة موافقة.

---

## 4. آليات منع التلاعب المالي (Anti-Fraud Controls)

هذه النقطة هي جوهر طلبك، وأقترح 5 آليات مجتمعة (وليس آلية واحدة) لأن أي واحدة بمفردها قابلة للثغرات:

1. **PIN رقمي خاص بكل كاشير (منفصل عن كلمة مرور الحساب)**
   كل حركة حساسة (حذف طلب، استرجاع، تجاوز حد الخصم) تطلب إدخال PIN. الـPIN يُسجَّل مع الحركة في سجل التدقيق، فنعرف *من بالضبط* نفّذ العملية حتى لو الكل يستخدم نفس جهاز الكاشير.

2. **سجل تدقيق شامل (Immutable Audit Log)**
   جدول `audit_log` منفصل، **append-only** (لا تعديل ولا حذف حتى من الـAPI)، يسجّل: من، ماذا، متى، القيمة قبل/بعد، السبب المكتوب. هذا يحوّل أي نزاع من "كلمة ضد كلمة" إلى دليل رقمي.

3. **تسوية الوردية (Shift / Cash Drawer Reconciliation)**
   عند إغلاق وردية الكاشير: النظام يحسب (المبيعات النقدية المتوقعة من قاعدة البيانات) ويطلب من الكاشير إدخال (المبلغ الفعلي في الدرج). أي فرق يُسجَّل تلقائياً ويُرفع فوراً كتنبيه للمالك — بدون أي تدخل بشري لحظة الإغلاق.

4. **حدود تلقائية (Guardrails) بدل الاعتماد على الثقة**
   سقف خصم يومي/لكل طلب لكل كاشير، سقف عدد عمليات الإلغاء بالوردية الواحدة. تجاوز السقف لا يُرفض فقط، بل **يُنبّه المالك لحظياً**.

5. **كشف الأنماط الشاذة (Anomaly Detection) — أتمتة ذكية**
   مثال: كاشير معيّن نسبة إلغاءاته في هذه الوردية أعلى بـX% من متوسطه التاريخي → إشعار فوري للمالك "⚠️ نشاط غير معتاد: [اسم الكاشير] ألغى 5 طلبات خلال 20 دقيقة". هذا فرق جوهري عن الأنظمة التقليدية التي تكتفي بتسجيل البيانات دون تحليلها.

---

## 5. تدفق البيانات اللحظي (Real-time Data Flow)

المشروع فعلياً يملك بنية تحتية جاهزة لهذا: `AdminLayout` يشترك حالياً في قناة Supabase Realtime على جدول `orders` (`postgres_changes` على `INSERT`) لتشغيل صوت التنبيه. سنبني على نفس النمط ونوسّعه:

```
[جهاز الكاشير]                         [قناة Supabase Realtime]                [جهاز/جوال المالك]
      │                                          │                                     │
      ├─ طلب جديد يُدخله الكاشير ────────────▶  INSERT على orders  ───────────────▶  ظهور فوري في لوحة
      │                                          │  (فلترة restaurant_id)              المالك + رنين
      │                                          │
      ├─ تعديل مخزون (نفدت مادة) ─────────────▶  UPDATE على inventory_items ────▶  تحديث فوري لعداد
      │                                          │                                     المخزون + تنبيه
      │                                          │                                     "أوشك على النفاد"
      │                                          │
      ├─ محاولة حذف/استرجاع (PIN) ─────────────▶  INSERT على audit_log ──────────▶  تنبيه Push فوري:
      │                                          │                                     "كاشير X ألغى طلب"
      │                                          │
      └─ إغلاق وردية + فرق تسوية ───────────────▶  INSERT على shift_closures ─────▶  تنبيه فوري بالفرق
                                                                                       + ملخص الوردية
```

**المبدأ:** كل حدث يكتبه الكاشير في قاعدة البيانات يُبَث لحظياً (WebSocket عبر Supabase Realtime، بدون Polling) لأي جلسة مفتوحة للمالك — نفس التقنية المستخدمة حالياً للطلبات، فقط نوسّع نطاق الجداول المشتركة ونضيف تصنيف "مستوى الخطورة" (عادي / يستدعي انتباه / حرج) لتقرير هل يُصاحَب الحدث بإشعار صوتي/Push أم يكتفى بتحديث الشاشة بصمت.

---

## 6. سيناريوهات ذكية إضافية (أبعد من الواجهات التقليدية)

- **"وضع الوردية" بدل تسجيل خروج/دخول كامل:** الكاشير يبدأ يومه بفتح وردية (اسمه + PIN)، وينتقل بين الكاشيرية دون الخروج من جلسة الجهاز — أسرع تشغيلياً وأدق في التتبع من نظام "حساب واحد لكل جهاز".
- **تقرير وردية تلقائي عند الإغلاق:** يُرسل تلقائياً (واتساب/بريد/إشعار) للمالك فور إغلاق أي وردية: عدد الطلبات، الإجمالي، عدد الإلغاءات، فرق التسوية إن وجد — بدون أن يفتح المالك التطبيق أصلاً.
- **لوحة "Live Ops" مخصصة للمالك على الجوال:** ملخص حي (كم كاشير شغّال الآن، كم طلب بالساعة الأخيرة، هل في تنبيه حرج) منفصلة عن شاشة إدارة الكاشير المزدحمة.
- **قفل تلقائي مؤقت (Soft Lock):** إذا تجاوز كاشير عدد محاولات PIN خاطئة، تُقفل صلاحياته الحساسة مؤقتاً ويُنبَّه المالك، بدل قفل الحساب بالكامل (يستمر بالعمل العادي: استقبال ودفع).
- **مطابقة تلقائية بين المخزون والمبيعات (Auto Depletion):** كل عملية بيع تُنقص المخزون تلقائياً حسب "وصفة" الصنف (BOM)، فيعرف المالك لحظياً "هذا العجز الفعلي لا يطابق المفروض" دون جرد يدوي.
- **صلاحيات ديناميكية بالفرع/الفترة:** كاشير له صلاحية فرع معيّن فقط، أو حتى صلاحية محصورة بفترة زمنية (وردية اليوم فقط) — منع الدخول خارج نطاق عمله حتى لو سُرقت بياناته.

---

## 7. التغييرات التقنية المطلوبة (Data Model — للعلم فقط، لا تنفيذ الآن)

هذا يتطلب تحوّلاً جوهرياً من "مستخدم واحد = مطعم واحد" إلى "عدة مستخدمين لكل مطعم":

1. **جدول جديد `staff_members`**: `id, restaurant_id, auth_user_id, full_name, role (owner|cashier|shift_supervisor), pin_hash, is_active, created_at`.
2. **تعديل `AdminGuard` + `/api/admin/my-restaurant`**: بدل البحث بـ `owner_id` فقط، يبحث في `staff_members` عن أي مستخدم مرتبط بالمطعم، ويُعيد `role` ضمن الاستجابة.
3. **طبقة توجيه حسب الدور (Route Guard)**: صفحات مثل `admin/inventory`, `admin/statistics`, `admin/settings` تتحقق من الدور وتُخفي/تمنع الوصول لغير المالك — على مستوى الواجهة **و**على مستوى الـAPI (Row Level Security في Supabase، وليس فقط إخفاء زر في الواجهة، لأن إخفاء الزر وحده لا يمنع استدعاء الـAPI مباشرة).
4. **جدول `audit_log`**: append-only، بدون صلاحية `UPDATE`/`DELETE` حتى بمفتاح الـservice role إن أمكن (أو محمي بـ trigger يمنع التعديل).
5. **جدول `shift_sessions`**: فتح/إغلاق وردية، المبلغ الافتتاحي، المتوقع، الفعلي، الفرق.
6. **تعديل جدول `orders`**: إضافة `voided_by`, `void_reason`, `refunded_by` بدل الحذف الفعلي الحالي (`DELETE`) — التحويل من "حذف" إلى "إلغاء منطقي" (Soft Delete) هو شرط أساسي لأي نظام تدقيق مالي حقيقي.
7. **إدارة الكاشيرية من شاشة المالك:** واجهة جديدة تحت `admin/settings` أو صفحة مستقلة `admin/staff` لإضافة/تعطيل كاشير وتوليد/إعادة تعيين PIN.

---

## 8. خطة التنفيذ المقترحة (Phased Rollout)

| المرحلة | المحتوى | لماذا بهذا الترتيب |
|---|---|---|
| **1** | قاعدة البيانات: `staff_members`, `audit_log`, تعديل `orders` لـ soft-delete | أساس كل شيء لاحق، بدون كسر تسجيل الدخول الحالي (المالك يبقى يعمل كما هو) |
| **2** | تحديث تسجيل الدخول + `AdminGuard` ليدعم أدوار متعددة، وصفحة "إدارة الكاشيرية" | يتيح للمالك إنشاء أول حساب كاشير فعلياً |
| **3** | تطبيق مصفوفة الصلاحيات على كل صفحة/API (إخفاء + منع فعلي عبر RLS) | هذا ما يمنع "الكاشير يفتح الرابط مباشرة" |
| **4** | PIN + بوابات الموافقة (حذف/استرجاع/خصم) + الوردية والتسوية | الطبقة الأمنية المالية الأساسية |
| **5** | التنبيهات اللحظية للمالك + كشف الأنماط الشاذة + تقرير الوردية التلقائي | الطبقة الذكية/الأتمتة فوق الأساس الصلب |

كل مرحلة قابلة للنشر والاختبار بمفردها دون كسر عمل المطعم الحالي أثناء التطوير.

---

## الخلاصة

النظام الحالي يفتقر لأي فصل أدوار على مستوى قاعدة البيانات — وهذه ليست "ميزة ناقصة" بل **فجوة أمنية مالية حقيقية** بما أن حذف الطلبات (`DELETE`) يتم اليوم بلا قيد ولا تسجيل. الخطة أعلاه تحل هذا عبر: جدول مستخدمين متعدد بدل حساب واحد، مصفوفة صلاحيات مطبّقة على الواجهة والـAPI معاً، PIN + سجل تدقيق + تسوية ورديات لمنع التلاعب، وتنبيهات لحظية ذكية تجعل المالك يرى كل حركة حساسة فور حدوثها بدل اكتشافها بعد أيام.

**بانتظار أمرك بكلمة "ابدأ" للبدء بالتنفيذ (المرحلة 1).**
