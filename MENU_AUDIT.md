# تقرير تحليل منيو العملاء — نقاط ناقصة ومقترحات

> **تاريخ التقرير:** 2026-06-24  
> **الصفحات المدروسة:** المنيو، السلة، تتبع الطلب، طلباتي، معلوماتي

---

## أولاً — أخطاء فعلية في الكود (Bugs)

---

### 🔴 خطأ 1: رسالة "المطعم مغلق" لا تظهر أبداً

**الوضع الحالي:**  
عندما يكون المطعم مغلقاً ويضغط الزبون على أي وجبة، البرنامج يحاول يشغّل رسالة تنبيه (toast)، لكن هذه الرسالة **غير موجودة في الشاشة** — يعني البرنامج يحاول يعرضها بس ما عمل الشاشة نفسها. الزبون يضغط ولا يصير شيء، ويتساءل "ليش ما تضاف الوجبة؟".

**الملف:** `src/app/home/HomeClient.tsx` — السطر 119 (state موجود) لكن لا يوجد UI يعرضه.

**الحل المقترح:**  
نضيف رسالة Toast صغيرة تطلع من الأسفل وتختفي بعد ثانيتين تقول "المطعم مغلق حالياً".

---

### 🔴 خطأ 2: سعر الإضافات لا يُحسب في إجمالي السلة

**الوضع الحالي:**  
عندما يفتح الزبون وجبة ويختار إضافات (مثلاً: صوص إضافي بـ 500 دينار)، يشوف السعر الصح في نافذة الوجبة. لكن لما يضغط "إضافة للسلة"، **السعر المضاف هو سعر الوجبة فقط بدون الإضافات**. يعني السلة تجمع غلط والزبون يدفع أقل مما اختار.

**الملف:** `src/app/home/HomeClient.tsx` — دالة `handleAdd` وزر العداد في المودال.

**الحل المقترح:**  
نجمع سعر الإضافات المختارة مع سعر الوجبة الأساسي قبل ما ننزل للسلة.

---

## ثانياً — ميزات ناقصة مهمة جداً

---

### 🟠 نقطة 3: لا يوجد بحث في المنيو

**الوضع الحالي:**  
إذا المنيو فيه 50 وجبة والزبون يعرف اسم الوجبة اللي يريدها، لازم يتمرر يمين ويسار في كل الفئات حتى يلاقيها. **لا يوجد شريط بحث**.

**الملف:** `src/app/home/HomeClient.tsx`

**الحل المقترح:**  
نضيف شريط بحث صغير أسفل فئات التصفية، لما الزبون يكتب فيه تظهر فقط الوجبات اللي تحتوي على الكلمة المكتوبة.

---

### 🟠 نقطة 4: إشعارات الزبون لما تتغير حالة طلبه

**الوضع الحالي:**  
الزبون يرسل طلبه ثم يجب عليه يفتح التطبيق يدوياً ويدخل "تتبع طلبك" ليشوف هل الطلب قُبل أو لحسن قيد التجهيز. **لا يصله أي إشعار تلقائي**.

**ملاحظة:** الكود فيه API للإشعارات (`/api/push/notify`) لكنه غير مربوط بجانب الزبون.

**الحل المقترح:**  
- نضيف زر "فعّل الإشعارات" في صفحة تتبع الطلب أو بعد إرسال الطلب مباشرة
- لما المطعم يغير حالة الطلب، يوصل للزبون إشعار مباشرة على جهازه

---

### 🟠 نقطة 5: لا يوجد تواصل مع المطعم من صفحة التتبع

**الوضع الحالي:**  
الزبون يتابع طلبه ولو واجهته مشكلة (مثلاً الطلب تأخر أو محتاج يعدّل) **لا يوجد زر يتصل أو يراسل المطعم** من داخل صفحة التتبع.

**الملف:** `src/app/track/page.tsx`

**الحل المقترح:**  
نضيف زر واتساب يأخذ رقم المطعم المحفوظ في الإعدادات ويفتح محادثة مباشرة.

---

### 🟠 نقطة 6: الزبون لا يستطيع إلغاء طلبه

**الوضع الحالي:**  
بعد ما يرسل الزبون طلبه ويكتشف خطأ (مثلاً دخل العنوان الغلط)، **لا يوجد زر إلغاء** من جانب الزبون. يحتاج يتصل بالمطعم وهم يلغونه يدوياً.

**الملف:** `src/app/track/page.tsx` و `src/app/orders/page.tsx`

**الحل المقترح:**  
نضيف زر "إلغاء الطلب" يظهر فقط لما الطلب بحالة "انتظار" (pending) ولمدة 2-3 دقائق من وقت الإرسال.

---

### 🟠 نقطة 7: إعداد ملف التطبيق (PWA) خاطئ

**الوضع الحالي:**  
عندما يضيف الزبون التطبيق على شاشته الرئيسية، التطبيق يفتح على **صفحة لوحة تحكم السائق** بدل منيو المطعم. السبب أن ملف `manifest.json` مضبوط للسائقين. كذلك اسم التطبيق "CulinaShare" بدل اسم المطعم.

**الملف:** `public/manifest.json`

**الحل المقترح:**  
نصلح `start_url` ليوجه لصفحة المنيو الصح، ونجعل اسم التطبيق يأخذ اسم المطعم تلقائياً.

---

## ثالثاً — تحسينات تجربة المستخدم (UX)

---

### 🟡 نقطة 8: لا يوجد عداد السلة على أيقونة المنيو في الشريط السفلي

**الوضع الحالي:**  
الشريط السفلي فيه 4 أيقونات (المنيو، تتبع طلبك، طلباتي، معلوماتي). لما الزبون يضيف وجبات للسلة وينتقل لصفحة ثانية، **لا يوجد مؤشر** يذكره بأن عنده وجبات في السلة.

**الملف:** `src/components/BottomNav.tsx`

**الحل المقترح:**  
نضيف نقطة حمراء صغيرة (badge) فوق أيقونة المنيو تعرض عدد الوجبات في السلة.

---

### 🟡 نقطة 9: لا يوجد زر "للأعلى" عند التمرير للأسفل

**الوضع الحالي:**  
لو المنيو طويل والزبون تمرر للأسفل كثيراً، يحتاج يتمرر للأعلى يدوياً حتى يصل لشريط الفئات ويختار فئة ثانية.

**الملف:** `src/app/home/HomeClient.tsx`

**الحل المقترح:**  
نضيف زر دائري صغير يظهر عند التمرير لأسفل، لما يضغطه يرجع للأعلى مباشرة.

---

### 🟡 نقطة 10: صورة بديلة للوجبات بدون صورة غير لائقة

**الوضع الحالي:**  
الوجبات التي ليس لها صورة تعرض رابطاً خارجياً (`placehold.co`) يعرض كلمة "?" أو نص في وسط صورة رمادية. الاتصال بالإنترنت مطلوب حتى للصورة البديلة.

**الملف:** `src/app/home/HomeClient.tsx` — السطرين 440 و 622.

**الحل المقترح:**  
نستبدل الصورة البديلة بأيقونة محلية (من Lucide) داخل مربع ملوّن بلون المطعم.

---

### 🟡 نقطة 11: صفحة "معلوماتي" لا تعرض المواقع المحفوظة

**الوضع الحالي:**  
أضفنا ميزة حفظ المواقع وهي تظهر في صفحة السلة فقط. لكن في صفحة "معلوماتي" **لا يستطيع الزبون يشوف أو يحذف مواقعه المحفوظة** إلا عند الطلب.

**الملف:** `src/app/profile/page.tsx`

**الحل المقترح:**  
نضيف قسم "مواقع التوصيل المحفوظة" في صفحة معلوماتي يعرض الكروت مع إمكانية الحذف.

---

### 🟡 نقطة 12: نظام تسجيل الدخول غير احترافي

**الوضع الحالي:**  
نظام تسجيل الدخول يحول رقم الهاتف لإيميل وهمي (`07700000000@c.delivery`) ويستخدمه كحساب. هذا الأسلوب:
- لا يوجد فيه استعادة كلمة المرور
- الزبون لو نسي كلمة مروره لا يقدر يستعيد حسابه
- مو احترافي ومشكوك فيه من ناحية الأمان

**الملف:** `src/app/profile/page.tsx` و `src/app/cart/page.tsx`

**الحل المقترح:**  
الحل الأبسط: نعتمد كلياً على الحفظ المحلي (localStorage) بدون حسابات — لأن الزبون لا يحتاج حساباً أصلاً. أو ننتقل لنظام OTP (رمز SMS) إذا أردنا حسابات حقيقية.

---

### 🟡 نقطة 13: لا يوجد تفاصيل تسليم قبل الطلب

**الوضع الحالي:**  
الزبون لا يعرف قبل ما يرسل طلبه:
- ما هو الحد الأدنى للطلب؟
- ما هي رسوم التوصيل؟
- ما هو الوقت التقديري؟

**الحل المقترح:**  
نضيف هذه المعلومات في إعدادات المطعم (داشبورد) ونعرضها في المنيو وفي صفحة السلة قبل الإرسال.

---

## رابعاً — مشاكل تقنية

---

### ⚪ نقطة 14: متغير غير مستخدم في HomeClient

**الملف:** `src/app/home/HomeClient.tsx` السطر 83  
```js
const p = brandColor; // هذا المتغير معرّف ولكن لا يُستخدم في أي مكان
```
ليس خطأ كبيراً لكنه يدل على كود قديم نسي يُحذف.

---

### ⚪ نقطة 15: أيقونة التطبيق (PWA) هي favicon فقط

**الوضع الحالي:**  
ملف `manifest.json` يحدد أيقونة التطبيق كـ `favicon.ico` فقط. هذا يعني لما يثبّت الزبون التطبيق على جهازه تكون الأيقونة صغيرة وغير واضحة على شاشات اليوم.

**الحل المقترح:**  
نضيف أيقونات بأحجام متعددة (192×192 و 512×512) بصيغة PNG.

---

## ملخص الأولويات

| الأولوية | النقطة | السبب |
|----------|--------|-------|
| 🔴 عاجل | خطأ سعر الإضافات | يسبب خسارة مالية للمطعم |
| 🔴 عاجل | toast المطعم المغلق | الزبون محتار ليش ما يشتغل |
| 🟠 مهم | بحث في المنيو | تحسين كبير لتجربة الزبون |
| 🟠 مهم | إشعارات تغيير الطلب | يوفر على الزبون الانتظار المجهول |
| 🟠 مهم | تواصل مع المطعم من التتبع | للطوارئ والاستفسارات |
| 🟠 مهم | إلغاء الطلب | حق أساسي للزبون |
| 🟠 مهم | إصلاح PWA manifest | التطبيق يفتح على صفحة الغلط |
| 🟡 متوسط | عداد السلة على BottomNav | تذكير مستمر للزبون |
| 🟡 متوسط | زر "للأعلى" | راحة للتنقل في منيو طويل |
| 🟡 متوسط | صورة بديلة محلية | لا يعتمد على إنترنت خارجي |
| 🟡 متوسط | مواقع محفوظة في "معلوماتي" | إدارة أسهل للمواقع |
| 🟡 متوسط | تفاصيل التوصيل قبل الطلب | شفافية أكثر |
| ⚪ منخفض | تنظيف متغير `p` غير المستخدم | نظافة الكود |
| ⚪ منخفض | أيقونات PWA بأحجام صحيحة | جودة أفضل عند التثبيت |
