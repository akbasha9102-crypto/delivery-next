# ميزة أنواع الطلب (توصيل / طلب داخلي / استلام سفري)

## شنو سوينا بالضبط

### 1. صفحة الدفع — `src/app/cart/page.tsx`
أضفنا مبدّل (Tabs) بأعلى فورم "معلومات الطلب" فيه 3 خيارات:
- 🛵 **توصيل** — نفس الفورم القديم بالضبط (اسم، هاتف، خريطة، عنوان) بدون أي تغيير بالمنطق.
- 🍽️ **طلب داخلي** — يختفي العنوان والخريطة، ويظهر بدلاً عنهم حقل "رقم الطاولة".
- 🛍️ **استلام سفري** — يختفي العنوان والخريطة، ويبقى فقط الاسم ورقم الهاتف.

نافذة التأكيد قبل الإرسال تتكيف بنفس الطريقة (تخفي الخريطة/العنوان لغير التوصيل، وتعرض رقم الطاولة للطلب الداخلي).

عند إرسال الطلب، يُحفظ في قاعدة البيانات: `order_type` (delivery / dine_in / pickup) و `table_number` (فقط للطلب الداخلي).

### 2. قاعدة البيانات — `supabase_migration_order_type.sql`
كود SQL جاهز يضيف عمودين لجدول `orders`:
- `order_type` — نص، يقبل فقط: `delivery` أو `dine_in` أو `pickup`، وقيمته الافتراضية `delivery`.
- `table_number` — رقم، لرقم الطاولة.

**كلا العمودين Nullable** (يقبلون فارغ) — فالطلبات القديمة ما راح تتأثر أبداً.

### 3. داشبورد الطلبات — `src/app/admin/orders/page.tsx`
أضفنا تبويب بأعلى صفحة الطلبات يفصل العرض:
- **توصيل خارجي 🛵** — الطلبات اللي `order_type = delivery` (أو فاضية، للطلبات القديمة).
- **داخلي 🍽️ 🛍️** — الطلبات اللي `order_type = dine_in` أو `pickup`.

بطاقة الطلب الداخلي تعرض رقم الطاولة، وبطاقة السفري تعرض شارة "استلام سفري". وتم إخفاء خيار "إرسال للسائق" بالطلبات الداخلية لأنها ما تحتاج توصيل.

---

## شنو المطلوب منك حتى يشتغل زين

### خطوة وحيدة لا بد منها: تشغيل كود SQL في Supabase

1. افتح مشروعك بـ Supabase.
2. روح لـ **SQL Editor** من القائمة الجانبية.
3. افتح ملف `supabase_migration_order_type.sql` الموجود بالمشروع، وانسخ محتواه كامل.
4. الصقه بالـ SQL Editor واضغط **Run**.

بعدها الميزة تكون شغالة فوراً بدون أي إعادة تشغيل أو تعديل إضافي — لأن الكود بالموقع (Next.js) جاهز ومرفوع على GitHub.

### ملاحظة مهمة
إذا كنت شغّلت نسخة قديمة من هذا الملف بالسابق (النسخة اللي بس تضيف `order_type` بدون `table_number`)، لا مشكلة — الكود الجديد آمن يتكرر تشغيله (`IF NOT EXISTS`) وراح يضيف بس الناقص (`table_number` + الشرط `CHECK`) بدون ما يأثر على أي بيانات موجودة.

### كيف تتأكد إنها اشتغلت
- افتح صفحة الطلب من جهة الزبون (`/cart`) وجرب اختيار "طلب داخلي" أو "استلام سفري" وكمل الطلب.
- افتح داشبورد الطلبات (`/admin/orders`) وشوف تبويب "داخلي" — لازم يظهر فيه الطلب اللي سويته.
