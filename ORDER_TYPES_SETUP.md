# ميزة أنواع الطلب (توصيل / طلب داخلي / استلام سفري)

## شنو سوينا بالضبط

### 1. صفحة الدفع — `src/app/cart/page.tsx`
أضفنا مبدّل (Tabs) بأعلى فورم "معلومات الطلب" فيه 3 خيارات:
- 🛵 **توصيل** — نفس الفورم القديم بالضبط (اسم، هاتف، خريطة، عنوان) بدون أي تغيير بالمنطق.
- 🍽️ **طلب داخلي** — يختفي العنوان والخريطة، ويظهر بدلاً عنهم حقل "رقم الطاولة".
- 🛍️ **استلام سفري** — يختفي العنوان والخريطة، ويبقى فقط الاسم ورقم الهاتف.

نافذة التأكيد قبل الإرسال تتكيف بنفس الطريقة (تخفي الخريطة/العنوان لغير التوصيل، وتعرض رقم الطاولة للطلب الداخلي).

عند إرسال الطلب، يُحفظ في قاعدة البيانات: `order_type` (delivery / dine_in / pickup) و `table_number` (فقط للطلب الداخلي).

### 2. قاعدة البيانات — `supabase_migration_order_type.sql`
كود SQL جاهز يضيف عمودين لجدول `orders`:
- `order_type` — نص، يقبل فقط: `delivery` أو `dine_in` أو `pickup` أو `local` (كاشير المحل)، وقيمته الافتراضية `delivery`.
- `table_number` — رقم، لرقم الطاولة.

**كلا العمودين Nullable** (يقبلون فارغ) — فالطلبات القديمة ما راح تتأثر أبداً.

### 3. داشبورد الطلبات — `src/app/admin/dashboard/page.tsx`
هذي هي شاشة الطلبات الفعلية اللي يستخدمها المطعم (الصفحة القديمة `admin/orders/page.tsx` كانت ميتة — ما أحد يوصلها من التطبيق — فتم حذفها والمنطق انتقل هنا).

أضفنا تبويب بأعلى الصفحة يفصل العرض:
- **توصيل خارجي 🛵** — الطلبات اللي `order_type = delivery` (أو فاضية، للطلبات القديمة).
- **داخلي 🍽️ 🛍️** — الطلبات اللي `order_type = dine_in` أو `pickup`.

بطاقة الطلب الداخلي تعرض رقم الطاولة، وبطاقة السفري تعرض شارة "استلام سفري". طلبات `order_type = local` (كاشير نقطة البيع) مستبعدة كلياً من هذي الشاشة.

مسار الطلبات الداخلية نفس مسار التوصيل بالضبط، لكن بدون تاب "انتظار السائق": **واردة → تجهيز → جاهز → مكتمل**. تاب "جاهز" هنا يعني الطلب خلص تجهيز وصار جاهز على الطاولة/للاستلام — المطعم نفسه يضغط "الطلب جاهز ✓" ثم "تسليم الطلب ✓" (ما فيه سائق يسوي هذا تلقائياً كما بالتوصيل)، وبدون أي إشعارات دفع للسائقين بأي مرحلة، لأنها أصلاً ما تحتاج توصيل. تطبيق السائق نفسه صار يستبعد هذي الطلبات من قائمة "الطلبات الواردة" حتى ما يقدر سائق يستلمها بالغلط.

### 4. صفحات الزبون — تتبع الطلب وطلباتي
- `src/app/track/page.tsx`: شريط تتبع الطلب صار يتكيف حسب `order_type` — النصوص والأيقونات لمرحلة "جاهز"/"مكتمل" تختلف للطلب الداخلي (تم الاستلام) عن التوصيل (تم التوصيل)، وخريطة تتبع السائق ما تظهر إلا للتوصيل. بطاقة "تفاصيل الطلب" تعرض رقم الطاولة بدل المنطقة للطلب الداخلي، وما تعرض أي منهما للسفري.
- `src/app/orders/page.tsx` (طلباتي): نفس منطق التسميات حسب `order_type`، وزر "اطلب مجدداً" لطلب داخلي/سفري سابق يتخطى خطوة تحديد الموقع بالخريطة تماماً ويرسل الطلب الجديد بنفس `order_type` (ورقم الطاولة إذا داخلي) مباشرة.

---

## شنو المطلوب منك حتى يشتغل زين

### خطوتان لا بد منهما: تشغيل كود SQL في Supabase

1. افتح مشروعك بـ Supabase.
2. روح لـ **SQL Editor** من القائمة الجانبية.
3. افتح ملف `supabase_migration_order_type.sql` (محدّث ليشمل `local` بقيد الـ CHECK — شغّله حتى لو شغّلت نسخة أقدم منه سابقاً، آمن يتكرر) وانسخ محتواه كامل، الصقه واضغط **Run**.
4. افتح ملف `supabase_migration_no_driver_on_internal_orders.sql` (جديد — حماية تمنع تعيين سائق لأي طلب داخلي/سفري/محلي على مستوى القاعدة نفسها) وكرر نفس الخطوة.

بعدها الميزة تكون شغالة فوراً بدون أي إعادة تشغيل أو تعديل إضافي — لأن الكود بالموقع (Next.js) جاهز ومرفوع على GitHub.

### ملاحظة مهمة
كل ملفات الـ SQL بهذا المشروع آمنة تتكرر تشغيلها (`IF NOT EXISTS` / `DROP CONSTRAINT IF EXISTS` ثم إعادة الإضافة) بدون ما تأثر على أي بيانات موجودة.

### تنبيه: تطبيق السائق لسه ما وصله إصلاح كامل
فيه إصلاح إضافي مخطط له بملف `src/app/driver/dashboard/page.tsx` (يمنع ظهور الطلبات الداخلية/السفرية بقائمة "الطلبات الواردة" عند السائق) — ما قدرنا نرفعه لأن الملف فيه تعديلات أخرى غير مرتبطة لسه ما انحفظت (commit). قيد الـ CHECK الجديد (`orders_no_driver_on_internal_check`) يمنع أي ضرر فعلي بالبيانات لو صار خطأ، لكن يفضّل إكمال هذا الإصلاح لاحقاً.

### كيف تتأكد إنها اشتغلت
- افتح صفحة الطلب من جهة الزبون (`/cart`) وجرب اختيار "طلب داخلي" أو "استلام سفري" وكمل الطلب.
- افتح داشبورد الطلبات (`/admin/dashboard`) وشوف تبويب "داخلي" — لازم يظهر فيه الطلب اللي سويته.
- افتح `/track` أو `/orders` من جهة الزبون بنفس رقم الهاتف وتأكد إن النصوص والتفاصيل (رقم الطاولة، حالة "جاهز"/"تم الاستلام"، بدون خريطة سائق) تظهر صح للطلب الداخلي/السفري.
