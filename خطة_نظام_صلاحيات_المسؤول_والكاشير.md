# خطة معمارية: فصل صلاحيات "المسؤول" و"الكاشير" في داشبورد المطعم

> **حالة الوثيقة:** خطة للمراجعة فقط — لا تنفيذ حتى الآن.
> بعد موافقتك، أخبرني بكلمة **"ابدأ"** وسأبدأ بالتنفيذ حسب المراحل أدناه.

---

## 1. تحليل الوضع الحالي (Current State)

فحصت الكود الفعلي قبل كتابة أي خطة. هذا ما وجدته:

- **حساب واحد لكل مطعم فقط.** تسجيل الدخول (`src/app/login/page.tsx`) يستخدم Supabase Auth بحساب واحد: `slug@dasha.app`. أي شخص يملك هذه الكلمة (المالك، أو أي كاشير يعطيه إياها) يدخل بنفس الصلاحيات الكاملة، ولا يوجد أي تمييز بين "من دخل".
- **لا يوجد أي مفهوم أدوار (Roles) حالياً.** جدول `profiles(id, role, name, phone)` المذكور في `PRINCIPLES.md` **غير موجود فعلياً في قاعدة البيانات** (لا migration ينشئه) وغير مستخدم في أي سطر كود — أي أننا نبدأ من صفحة بيضاء تماماً لموضوع الأدوار، وهذه ميزة: لا يوجد كود قديم نكسره.
- **`AdminGuard.tsx`** يتحقق فقط: هل يوجد Session؟ → هل عنده مطعم عبر `owner_id`؟ → هل المطعم موقوف (`is_suspended`)؟ لا يوجد أي فحص أدوار بعد ذلك.
- **RLS (Row Level Security) شبه معطّل على أغلب الجداول** (`qual: true` على `orders`, `categories`, `items`, `drivers` حسب `PRINCIPLES.md`). فقط جداول المخزون الجديدة (`inventory_items`, `menu_recipes`, `stock_movements`) فيها RLS حقيقي عبر دالة `is_restaurant_owner(restaurant_id)`.
- **نقطة حرجة يجب أن تكون واضحة من البداية:** إخفاء زر في الواجهة (React) **ليس أماناً حقيقياً**. أي حساب كاشير سيملك JWT صالح من Supabase، وبما أن RLS مفتوح على `orders`/`items`/إلخ، فبإمكانه تجاوز الواجهة والوصول مباشرة لقاعدة البيانات إن لم نُطبّق الصلاحيات في RLS أيضاً. لذلك خطة اليوم تتعامل مع الصلاحيات على **طبقتين معاً**: الواجهة (UX/UI) وقاعدة البيانات (RLS) — وإلا فالفصل شكلي فقط وقابل للاختراق.
- **بنية جيدة موجودة يمكن البناء عليها:**
  - `stock_movements.performed_by → auth.users` — جاهز لتسجيل مين عمل الحركة.
  - نمط `is_restaurant_owner()` في migration المخزون — نفس النمط سنوسّعه للأدوار.
  - Realtime عبر `supabase.channel(...).on('postgres_changes', ...)` مستخدم فعلياً بالفعل (جرس الطلب الجديد في `admin/layout.tsx`) — هذه نفس الآلية التي سنستخدمها لبث التنبيهات اللحظية بين الكاشير والمسؤول.
  - بنية Push Notifications جاهزة (`api/push/broadcast`, `api/push/notify`) — يمكن إعادة استخدامها لإشعارات الموافقة الفورية للمالك على جواله.
  - `restaurants.owner_id` ليس فريداً — يعني نفس المالك يمكن أن يملك أكثر من صف `restaurant` (أكثر من فرع) من دون أي تعديل في قاعدة البيانات. هذا يعطينا **دعم "الفروع" مجاناً** إذا صممنا شاشة المالك لتجمع كل المطاعم التي `owner_id = uid`.

---

## 2. الفلسفة المعمارية

بدل تصميم تقليدي (شاشتين منفصلتين "أدمن" و"كاشير")، أقترح نموذج **Progressive Disclosure**:
نفس الداشبورد، لكن كل عنصر واجهة وكل استعلام بيانات "يسأل" عن دور المستخدم الحالي قبل أن يُعرض أو يُنفَّذ. هذا يقلل الصيانة (لا صفحتين منفصلتين تتشعبان مع الوقت) ويسمح بإضافة أدوار مستقبلية (مثلاً "محاسب" أو "مدير فرع") بسهولة.

### آلية الدخول المقترحة (الأهم عملياً): PIN على مستوى الجهاز + حساب Supabase على مستوى المطعم

هذا هو الجزء الذي أطلب فيه أن أخرج عن "الواجهة التقليدية" كما طلبت:

- **لا تُنشئ حساب Supabase Auth منفصل لكل كاشير.** في الواقع العملي، الكاشيرية يتغيّرون كثيراً (دوام جزئي، تسريح، تأخير)، وإدارة حساب Auth كامل (بريد + كلمة مرور) لكل واحد فيهم عبء إداري غير ضروري ويُبطئ فتح الوردية.
- بدلاً من ذلك: الجهاز/التابلت في المطعم يسجّل دخول **مرة واحدة** بحساب المطعم الحالي (نفس آلية اليوم، هذا الحساب يُصبح تلقائياً دور **"مالك/مسؤول"**).
- عند فتح شاشة الكاشير أو تنفيذ أي عملية حساسة، يُطلب **PIN من 4-6 أرقام** خاص بكل موظف (مخزَّن Hash وليس نصاً صريحاً، خلافاً لمشكلة `admin_password` الحالية). الـ PIN يحدد:
  - هوية من نفّذ العملية فعلياً (للتدقيق والمحاسبة على الوردية).
  - دوره (كاشير محدود الصلاحيات).
- المالك يفتح صلاحيات إضافية (تقارير الأرباح، الإعدادات، إدارة المخزون بالتكلفة) بإدخال **PIN المالك** نفسه أو عبر جلسته الأساسية (Supabase session) مباشرة إذا كان هو من سجّل الدخول على الجهاز.
- **خيار إضافي اختياري (مرحلة لاحقة):** حساب Supabase Auth حقيقي لكل مدير فرع يريد الدخول من بيته/جواله عن بُعد لمتابعة التقارير — هذا فقط لدور "مسؤول/مدير"، وليس للكاشير الذي يعمل من داخل المحل غالباً.

**لماذا هذا أفضل من إنشاء حساب Auth لكل كاشير:**
سرعة تبديل الموظف بين وردية وأخرى (ثوانٍ بدل تسجيل خروج/دخول كامل)، توافق مع سلوك أنظمة POS العالمية (Toast, Square, Lightspeed كلها تستخدم PIN switch على نفس الجهاز)، وتقليل تعقيد إدارة المستخدمين لصاحب مطعم صغير لا يريد "يفتح إيميلات" لموظفيه.

---

## 3. نموذج البيانات الجديد (إضافي بالكامل — Additive)

طبقاً للمبدأ الذهبي في `PRINCIPLES.md` (Code أولاً، ثم Migration، ثم RLS)، هذه جداول **جديدة فقط** — لا تعديل ولا حذف على أي عمود موجود:

```sql
-- الموظفون المرتبطون بكل مطعم (كاشير أو مدير إضافي)
CREATE TABLE restaurant_staff (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  restaurant_id   UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  display_name    TEXT NOT NULL,
  role            TEXT NOT NULL CHECK (role IN ('owner', 'manager', 'cashier')),
  pin_hash        TEXT NOT NULL,              -- bcrypt/argon2، ليس نصاً صريحاً
  auth_user_id    UUID REFERENCES auth.users(id) ON DELETE SET NULL, -- اختياري: لدخول عن بعد
  is_active       BOOLEAN NOT NULL DEFAULT TRUE,
  max_discount_pct        NUMERIC(5,2) DEFAULT 0,   -- سقف الخصم المسموح دون موافقة
  max_void_amount         NUMERIC(12,2) DEFAULT 0,  -- سقف الإلغاء/الاسترجاع دون موافقة
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- ورديات الكاشير (فتح/إغلاق + جرد الكاش)
CREATE TABLE cashier_shifts (
  id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  restaurant_id         UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  staff_id              UUID NOT NULL REFERENCES restaurant_staff(id),
  opening_cash          NUMERIC(12,2) NOT NULL DEFAULT 0,
  expected_closing_cash NUMERIC(12,2),          -- محسوبة تلقائياً من مجموع الطلبات النقدية
  actual_closing_cash   NUMERIC(12,2),          -- يُدخلها الكاشير يدوياً عند الإغلاق
  variance              NUMERIC(12,2),          -- الفرق — يُنبَّه المالك تلقائياً لو تجاوز حداً معيناً
  opened_at             TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  closed_at             TIMESTAMPTZ,
  status                TEXT NOT NULL DEFAULT 'open' CHECK (status IN ('open','closed'))
);

-- طلبات الموافقة الفورية (Void كبير / خصم يتجاوز الحد / استرجاع)
CREATE TABLE approval_requests (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  restaurant_id   UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  requested_by    UUID NOT NULL REFERENCES restaurant_staff(id),
  request_type    TEXT NOT NULL CHECK (request_type IN ('void_order','refund','discount_override','price_override')),
  order_id        UUID REFERENCES orders(id),
  amount          NUMERIC(12,2),
  reason          TEXT,
  status          TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected')),
  resolved_by     UUID REFERENCES restaurant_staff(id),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  resolved_at     TIMESTAMPTZ
);

-- سجل تدقيق شامل — كل عملية حساسة تُسجَّل هنا
CREATE TABLE staff_actions_log (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  restaurant_id   UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  staff_id        UUID REFERENCES restaurant_staff(id),
  action_type     TEXT NOT NULL,      -- 'order_void','order_refund','discount_applied','price_edit','inventory_adjust', ...
  entity_type     TEXT,
  entity_id       UUID,
  before_data     JSONB,
  after_data      JSONB,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

دالة مساعدة لـ RLS (بنفس نمط `is_restaurant_owner()` الموجود فعلاً):

```sql
CREATE OR REPLACE FUNCTION get_staff_role(p_restaurant_id UUID)
RETURNS TEXT AS $$
  SELECT CASE
    WHEN EXISTS (SELECT 1 FROM restaurants WHERE id = p_restaurant_id AND owner_id = auth.uid())
      THEN 'owner'
    ELSE (
      SELECT role FROM restaurant_staff
      WHERE restaurant_id = p_restaurant_id AND auth_user_id = auth.uid() AND is_active = TRUE
      LIMIT 1
    )
  END;
$$ LANGUAGE sql SECURITY DEFINER STABLE;
```

> ملاحظة مهمة: نظام الـ PIN يعمل **فوق** جلسة Supabase الحالية للمطعم (نفس الـ JWT)، وليس بديلاً عنها. الـ PIN يُحدَّد من هو الشخص الفعلي فقط لغرض التسجيل والتقييد داخل الواجهة، أما RLS في قاعدة البيانات فيستمر يتحقق من `auth.uid()` (جلسة المطعم) + دور مشتق من جدول `restaurant_staff` **حين نربط حساب Auth Auth حقيقي بالكاشير** (Phase لاحقة). في المرحلة الأولى (PIN فقط بدون Auth منفصل)، تطبيق RLS الصارم على مستوى "كاشير مقابل مالك" غير ممكن تقنياً لأن كلاهما يستخدمان نفس JWT — لذا سيكون **إنفاذ الصلاحيات في المرحلة الأولى على مستوى API الخادم (Next.js Route Handlers مع `supabaseAdmin`)** الذي يتحقق من PIN + الدور المخزَّن قبل تنفيذ أي عملية، وليس على العميل مباشرة. هذا أهم قرار معماري في الخطة وأشرحه أكثر في القسم 6.

---

## 4. مصفوفة الصلاحيات (RBAC Matrix)

| الوحدة / الميزة | المسؤول (Owner/Manager) | الكاشير (Cashier) |
|---|---|---|
| **لوحة القيادة (KPIs، الأرباح، الهامش)** | كامل، ولكل الفروع | لا يظهر إطلاقاً — فقط عدد/قيمة طلبات وردياته هو |
| **إنشاء طلب جديد (POS سريع)** | نعم | نعم — هذه وظيفته الأساسية |
| **تعديل سعر عنصر يدوياً داخل طلب** | نعم | لا |
| **إلغاء/حذف طلب (Void)** | نعم مباشرة | فقط ضمن `max_void_amount`، وأي شيء أعلى → `approval_requests` فوري |
| **استرجاع مبلغ (Refund)** | نعم مباشرة | يتطلب موافقة فورية دائماً (لا يوجد سقف تلقائي) |
| **الخصومات** | نسبة حرة + تعديل قواعد الخصم | خصم ≤ `max_discount_pct` بدون موافقة، وأعلى منه → طلب موافقة |
| **عرض كمية المخزون** | نعم | نعم (الكمية فقط) |
| **تكلفة الشراء / المورّد / سعر التكلفة** | نعم | **لا يظهر إطلاقاً** — هذا رقم مالي حساس (هامش الربح) |
| **تسجيل هدر/تلف مخزون (WASTE)** | نعم | نعم، لكن بسبب إلزامي ويُسجَّل باسمه في `stock_movements.performed_by` |
| **تعديل يدوي لكمية المخزون (ADJUSTMENT)** | نعم | لا — فقط عبر حركة مرتبطة بطلب فعلي |
| **إدارة المنيو (إضافة/حذف/تسعير صنف)** | نعم | لا — قراءة فقط أثناء البيع |
| **الإحصائيات/التقارير المالية (أرباح، مصاريف، هامش)** | كامل | ممنوع تماماً |
| **تقرير وردية شخصي (مبيعاته هو فقط)** | يرى تقارير الكل | يرى فقط ورديته |
| **إدارة السائقين (بيانات، حذف، إضافة)** | نعم | لا — يمكنه فقط اختيار سائق متاح لطلب معيّن |
| **إعدادات المطعم العامة (شعار، ألوان، أوقات الدوام)** | نعم | لا يظهر القسم إطلاقاً |
| **إدارة الموظفين (إضافة/تعطيل كاشير، تحديد الحدود)** | نعم فقط | لا يظهر القسم إطلاقاً |
| **الأرشيف (طلبات قديمة)** | كل الطلبات | طلبات ورديته فقط |
| **سجل التدقيق (Audit Log)** | يرى كل شيء لحظياً | لا يرى، لكن أفعاله كلها مسجَّلة فيه |
| **فتح/إغلاق الوردية وجرد الكاش** | يرى كل الورديات + الفروقات (Variance) لكل الكاشيرية | يفتح/يغلق وردية نفسه فقط |
| **إشعارات فورية يستلمها** | طلب موافقة معلّق، خصم/إلغاء كبير، نقص مخزون حرج، فرق كاش عند الإغلاق | طلب جديد له، نتيجة موافقة طلبه (قُبل/رُفض) |
| **عرض الفروع المتعددة** | يجمع كل المطاعم حيث `owner_id = uid` في شاشة واحدة | لا يوجد — مرتبط بفرعه فقط |

---

## 5. آليات منع التلاعب المالي (الجزء الأهم من طلبك)

1. **لا حذف حقيقي لأي طلب من قبل الكاشير أبداً.** "الإلغاء" هو تغيير حالة (`status = 'cancelled'`) + سبب إلزامي، وليس `DELETE`. الطلب الأصلي يبقى في قاعدة البيانات دائماً لأي طلب موافقة/تدقيق لاحق.
2. **حدود رقمية لكل كاشير** (`max_discount_pct`, `max_void_amount`) يضبطها المالك فقط، وأي تجاوز يُحوَّل تلقائياً لطلب موافقة فوري بدل الرفض الجامد — هذا يحافظ على سرعة العمل في الحالات المشروعة (زبون غاضب، طلب خاطئ) دون فتح ثغرة.
3. **كل عملية حساسة تُكتب في `staff_actions_log` مع `before_data`/`after_data`** — حتى لو نفّذها المالك نفسه، للشفافية والتاريخ الكامل.
4. **تسوية الكاش نهاية الوردية (Cash Reconciliation):** النظام يحسب `expected_closing_cash` تلقائياً من مجموع الطلبات النقدية المدفوعة خلال الوردية، ويقارنها بما يُدخله الكاشير يدوياً. أي فرق (`variance`) أعلى من حد معيّن (مثلاً 5000 د.ع أو ما يعادل نسبة صغيرة) يُرسل تنبيهاً فورياً للمالك مع تفاصيل الوردية.
5. **لا يرى الكاشير أبداً تكلفة الشراء أو هامش الربح** — حتى لو كان جزءاً من شاشة المخزون التي يستخدمها لتسجيل الاستهلاك اليومي.
6. **جلسة الكاشير تنتهي تلقائياً (auto-lock)** بعد فترة خمول قصيرة (مثلاً دقيقتين) وتطلب PIN من جديد — يمنع استخدام شخص آخر لجلسة كاشير مفتوحة.

---

## 6. الميزات الذكية والأتمتة (أبعد من الواجهة التقليدية)

- **موافقة فورية عن بُعد (Remote Instant Approval):** عند طلب استرجاع أو خصم كبير، يصل إشعار Push فوري لجوال المالك (عبر بنية `api/push/broadcast` الموجودة فعلاً) حتى لو كان خارج المطعم، فيوافق أو يرفض بضغطة واحدة من هاتفه، وتُحدَّث شاشة الكاشير تلقائياً عبر Realtime دون أن يحتاج أي طرف "يحدّث الصفحة".
- **تقرير Z تلقائي عند إغلاق كل وردية:** ملخص فوري (عدد الطلبات، الإجمالي، طرق الدفع، الخصومات الممنوحة، أي طلبات موافقة حدثت) يُرسل تلقائياً كإشعار أو يُحفظ كـ PDF/سجل بدون أي إدخال يدوي من المالك.
- **كشف الشذوذ (Anomaly Detection) خفيف الوزن:** مقارنة تلقائية لأداء كل كاشير مقابل متوسط الورديات المشابهة (نفس اليوم/الساعة) — تنبيه المالك إن كاشير معيّن لديه نسبة إلغاءات أو خصومات أعلى بشكل غير طبيعي من البقية، دون الحاجة لمراجعة يدوية لكل وردية.
- **رؤية موحّدة للفروع (Multi-Branch Rollup):** بما أن `owner_id` يمكن أن يملك أكثر من مطعم بدون أي تعديل بنيوي، شاشة المالك تعرض تبديل/تجميع فوري بين الفروع (إجمالي الأرباح لكل الفروع، ثم تفصيل لكل فرع) — ميزة "تدير الفروع" التي طلبتها تتحقق تقريباً مجاناً من البنية الحالية.
- **اقتراح إعادة الطلب التلقائي للمخزون:** بالاعتماد على `reorder_quantity` و`low_stock_alerts` الموجودين فعلاً في migration المخزون — تنبيه استباقي للمالك بدل انتظار نفاد المادة فعلياً.
- **تبديل الكاشير بالـ PIN بدل تسجيل خروج/دخول كامل** (مشروح في القسم 2) — يوفر ثواني بكل عملية تبديل وردية، وهذا فرق كبير عملياً على أرض المحل.

---

## 7. تدفق البيانات اللحظي (Real-time Data Flow)

### سيناريو أ: طلب جديد من الكاشير
```
الكاشير ينشئ طلب (POS)
   → INSERT في orders (restaurant_id, ...)
   → Supabase Realtime (postgres_changes: INSERT on orders)
   → قناة مشتركة بـ restaurant_id (نفس آلية admin/layout.tsx الحالية)
   → المالك: تحديث لحظي لعداد الطلبات + الجرس (كما هو اليوم، بدون تغيير)
   → الكاشير: تحديث فوري لشاشة "طلبات نشطة" الخاصة به
```

### سيناريو ب: تعديل/استهلاك مخزون بسبب طلب
```
الطلب يُقفل (status = 'completed')
   → trigger أو API يسجّل حركة في stock_movements (OUT_ORDER)
   → trg_apply_stock_movement (موجود فعلاً) يحدّث current_stock تلقائياً
   → Realtime على stock_movements/inventory_items
   → شاشة المخزون عند المالك تتحدث فوراً + تنبيه إن current_stock <= min_alert_stock
```

### سيناريو ج: طلب موافقة من الكاشير (Void/Refund/Discount كبير)
```
الكاشير يطلب استرجاع يتجاوز حده
   → INSERT في approval_requests (status='pending')
   → Realtime + Push إلى جهاز المالك فوراً
   → المالك يوافق/يرفض من أي مكان
   → UPDATE على approval_requests.status
   → Realtime يعيد فتح/يقفل الإجراء على شاشة الكاشير تلقائياً
   → تُسجَّل النتيجة في staff_actions_log
```

---

## 8. خطة التنفيذ المرحلية (Phased Rollout)

| المرحلة | المحتوى | الخطورة (حسب PRINCIPLES.md) |
|---|---|---|
| **1. قاعدة البيانات** | إنشاء الجداول الأربعة الجديدة (`restaurant_staff`, `cashier_shifts`, `approval_requests`, `staff_actions_log`) + دالة `get_staff_role()`. كلها إضافية بالكامل، صفر تعديل على جداول موجودة. | منخفض — لا تعديل على شيء قائم |
| **2. واجهة إدارة الموظفين** | شاشة جديدة في `admin/settings` (أو قسم مستقل) للمالك فقط: إضافة كاشير (اسم + PIN)، تحديد `max_discount_pct`/`max_void_amount`، تعطيل/تفعيل. | منخفض |
| **3. طبقة إنفاذ الصلاحيات في API** | كل Route Handler حساس (إلغاء طلب، خصم، تعديل مخزون بالتكلفة) يتحقق من الدور عبر `supabaseAdmin` + PIN قبل التنفيذ — هذا هو خط الدفاع الحقيقي بما أن RLS لا يستطيع التمييز بين المالك والكاشير في نفس الجلسة (PIN-only) بالمرحلة الأولى. | **متوسط** — يلمس مسارات مالية حساسة، يحتاج اختبار دقيق |
| **4. شاشة الكاشير المحدودة (POS View)** | واجهة مبسّطة تُخفي كل قسم لا يخص الكاشير حسب مصفوفة القسم 4، مع قفل تلقائي بعد خمول. | منخفض–متوسط |
| **5. الورديات وتسوية الكاش** | فتح/إغلاق وردية، حساب `expected_closing_cash`، تنبيه الفروقات. | متوسط |
| **6. الموافقات الفورية + الإشعارات الذكية** | `approval_requests` + ربطها بـ Push/Realtime الموجودَين فعلاً. | متوسط |
| **7. سجل التدقيق الكامل + كشف الشذوذ** | تفعيل `staff_actions_log` في كل نقطة حساسة + تقرير Z التلقائي. | منخفض |
| **8. (اختياري لاحقاً) حساب Auth حقيقي للمدراء عن بُعد + RLS صارم فعلي بالأدوار** | لتفعيل تمييز حقيقي على مستوى قاعدة البيانات (وليس فقط API)، يحتاج كل كاشير حساب Supabase Auth مستقل. قرار يُتَّخذ لاحقاً إذا احتجت دخول الكاشير من جهاز/مكان مختلف عن جهاز المحل. | عالٍ — يلمس RLS المفتوح حالياً، يتطلب وكيل تحليل مخاطر منفصل حسب القاعدة الذهبية |

كل مرحلة تُنفَّذ وتُختبر لوحدها قبل الانتقال للتي تليها، ولا شيء يكسر تسجيل الدخول الحالي لأي مطعم — المالك الحالي يستمر بنفس تجربته تماماً، والتغيير الوحيد أنه الآن "دور owner" ضمنياً.

---

## 9. الخلاصة

هذا تصميم مبني على قراءة فعلية للكود الحالي وليس افتراضات — استغل ما هو موجود فعلاً (نمط RLS الخاص بالمخزون، Realtime، Push، تعدد المطاعم لنفس `owner_id`) بدل إعادة اختراعه. الجزء الأكثر أهمية لقرارك: **آلية PIN بدل حساب Auth منفصل لكل كاشير** (قسم 2) — هذا قرار معماري يستحق نقاشك قبل البدء، لأنه يحدد شكل كل شيء بعده.

**بانتظار موافقتك.** قل "ابدأ" لأبدأ بالمرحلة 1، أو أخبرني إن أردت تعديل أي جزء من التصميم أولاً (خصوصاً قرار PIN مقابل حساب Auth منفصل).
