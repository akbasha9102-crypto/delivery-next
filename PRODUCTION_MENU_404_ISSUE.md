# مشكلة: صفحة المنيو ترجع 404 على الإنتاج (مثال: /menu/dari)

تاريخ التشخيص: 2026-07-08

## الأعراض

- فتح `https://delivery-next-beta.vercel.app/menu/<slug>` يرجّع صفحة
  "This page could not be found" (404) رغم إن المطعم موجود فعلياً بقاعدة البيانات.
- المشكلة عامة، مو خاصة بمطعم وحدة: جُرّبت عدة مطاعم (`dari`, `melano`, `test1`)
  وكلهم يعطون نفس النتيجة.
- نفس المشكلة صارت سابقاً (قبل يوم تقريباً) وانحلّت، لكن بدون توثيق للحل.

## التشخيص اللي تم عمله

1. **التأكد إن البيانات موجودة فعلاً بقاعدة البيانات** — تم الاتصال المباشر
   بمشروع Supabase (`gbmwrvnmvobvieembxmf`) باستخدام بيانات `.env.local`
   محلياً، وتأكدنا إن:
   - المطعم `dari` موجود (id, name, slug صحيحين).
   - عنده 4 فئات (categories) و 8 أصناف (items).
   - إعدادات المطعم (`restaurant_settings`) كاملة وسليمة (لوقو، كوبون، واتساب،
     ساعات عمل...).
   - **النتيجة: لا يوجد أي حذف أو نقص بالبيانات.**

2. **فحص كود الصفحة** (`src/app/menu/[slug]/page.tsx`) — الكود يبحث عن المطعم
   بـ `slug` عبر عميل Supabase بصلاحية `service_role` (لتجاوز RLS)، وإذا ما
   لقى صف يستدعي `notFound()`. اكتشفنا إن الكود كان **يتجاهل أي خطأ** يرجع من
   Supabase (لا يفحص `error`)، فأي فشل بالاستعلام كان يُعامَل بصمت كـ "المطعم
   مو موجود" ويطلع 404 مضلِّل. تم إصلاح هذا (commit `47d81f4`) بحيث أي خطأ
   حقيقي يطلع كـ Server Error بدل 404 مزيّف — هذا يساعد بتشخيص أدق بالمستقبل،
   لكنه **لا يحل** المشكلة الحالية نفسها.

3. **نشر نسخة جديدة (Redeploy) بعد الإصلاح** — النتيجة كانت **نفس 404**
   بالضبط، بدون أي خطأ يظهر. هذا يعني إن الاستعلام لقاعدة البيانات نجح تقنياً
   لكنه رجّع **صفر صفوف بصمت** — مو خطأ اتصال أو مفتاح غير صالح تماماً، بل
   نتيجة فارغة.

4. **استبعاد أسباب أخرى بالكود:**
   - لا يوجد `generateStaticParams` أو `dynamicParams = false` يحدد قائمة
     مطاعم ثابتة وقت البناء.
   - الـ `middleware.ts` لا يتدخل إطلاقاً بمسارات `/menu/*` (يقتصر على
     `/super-admin/dashboard`).
   - جُرّب النشر الجديد فعزل احتمال الكاش القديم (ISR) — نفس النتيجة.

## السبب المتوقّع (Root Cause الأرجح)

**مفتاح `SUPABASE_SERVICE_ROLE_KEY` في إعدادات Vercel (Production) غير
صحيح أو قديم** — على الأغلب انحطّ فيه بالغلط نفس مفتاح `anon` بدل
`service_role`، أو تغيّر/انلغى من جهة Supabase بدون تحديثه بـ Vercel.

**ليش هذا يفسّر العرض بالضبط:**
- جدول `restaurants` عليه RLS (Row Level Security) مفعّل، ومصمم إنه ما ينفتح
  إلا بمفتاح `service_role` (اللي يتجاوز RLS تلقائياً).
- إذا انحط مفتاح `anon` بالغلط مكان `service_role`، أي استعلام يوصل بصلاحية
  "زائر عادي"، وسياسات RLS تصفّي (تفلتر) كل الصفوف بصمت — بدون ما ترجع أي
  رسالة خطأ (`error: null`)، بس النتيجة تكون فاضية (`data: null` أو `[]`).
- هذا يطابق تماماً كل الملاحظات: بدون خطأ ظاهر، فاشل مع كل المطاعم بدون
  استثناء، ومنطقي إنه صار "قبل يوم" لنفس السبب لو تحرّك المفتاح بطريقة أو
  بأخرى (rotate، أو تحديث env، أو نسخ لصق غلط).

> ملاحظة: ما قدرنا نتأكد 100% من القيمة الحالية بـ Vercel لأن قراءة أسرار
> بيئة الإنتاج (Environment Variables) تتطلب صلاحيات لا يملكها الوكيل الآلي
> بهذا الإعداد — لازم تأكيد يدوي من صاحب الحساب.

## خطوات الحل (بالتفصيل)

### الخطوة 1: انسخ المفتاح الصحيح من Supabase

1. افتح [supabase.com](https://supabase.com) وسجّل دخول.
2. افتح مشروعك (اللي فيه بيانات المطاعم — المشروع بالرابط
   `gbmwrvnmvobvieembxmf.supabase.co`).
3. من القائمة الجانبية: **Project Settings** (⚙️) → **API**.
4. تحت قسم **Project API keys** رح تشوف صفّين:
   - `anon` `public` ← **ما تستخدم هذا**
   - `service_role` `secret` ← **هذا اللي نبيه**
5. اضغط أيقونة النسخ 📋 جنب `service_role` (ما يحتاج تشوفها بعينك، بس
   انسخها للـ clipboard).

### الخطوة 2: حدّث القيمة في Vercel

1. افتح [vercel.com](https://vercel.com) وسجّل دخول.
2. افتح مشروع **delivery-next**.
3. تبويب **Settings** (فوق) → **Environment Variables** (بالقائمة الجانبية).
4. دور بخانة البحث عن: `SUPABASE_SERVICE_ROLE_KEY`.
5. اضغط **⋯** (ثلاث نقاط) جنب المتغير → **Edit**.
6. امسح القيمة القديمة بالكامل، والصق القيمة الجديدة من الخطوة 1.
7. تأكد إن خانة **Production** ✅ محددة (مو بس Preview/Development).
8. اضغط **Save**.

### الخطوة 3: (اختياري لكن يُفضّل) تأكد من الرابط أيضاً

بنفس صفحة Environment Variables، دور عن `NEXT_PUBLIC_SUPABASE_URL` وتأكد
قيمتها Production هي بالضبط:

```
https://gbmwrvnmvobvieembxmf.supabase.co
```

### الخطوة 4: أعد النشر (Redeploy) — خطوة إجبارية

تعديل Environment Variable **لا يطبَّق تلقائياً** على نسخة موجودة، لازم
نشر جديد:

1. تبويب **Deployments**.
2. آخر Deployment بالأعلى (Production) → اضغط **⋯** → **Redeploy**.
3. انتظر لين تخلص العملية (تقريباً دقيقة، الحالة تصير **Ready**).

### الخطوة 5: تحقّق من الحل

افتح `https://delivery-next-beta.vercel.app/menu/dari` من متصفح (تصفح خاص
Incognito أفضل لتفادي أي كاش قديم بالمتصفح) — المفروض يفتح المنيو عادي.

## لو ما انحلّت بعد هالخطوات

إذا رجعت نفس المشكلة رغم تحديث المفتاح والنشر من جديد، الاحتمال التالي هو
إن `NEXT_PUBLIC_SUPABASE_URL` بالإنتاج يشاور على **مشروع Supabase مختلف
تماماً** (مو نفس المشروع اللي فيه بيانات المطاعم). بهالحالة الحل يكون إما:
- تصحيح الرابط ليطابق المشروع الصحيح، أو
- ترحيل (migrate) بيانات المطاعم للمشروع الصحيح المربوط بالإنتاج فعلياً.
