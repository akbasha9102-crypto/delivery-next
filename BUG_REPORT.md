# 🛠️ تقرير الفحص الشامل للأخطاء البرمجية - مشروع Delivery Next

> **نطاق الفحص:** أخطاء منطقية وأخطاء تشغيل (Logic & Runtime Bugs) فقط. **لا يتضمن هذا التقرير أي فحص أمني** — الفحص الأمني سيكون منفصلاً لاحقاً كما طُلب.
> **تاريخ الفحص:** 2026-07-28
> **المنهجية:** خمسة عملاء فحص متخصصون قرأوا الكود فعلياً سطراً بسطر (لا تخمين)، ثم عميل مشرف (Supervisor) أعاد التحقق يدوياً من أهم النتائج الحرجة عبر قراءة الملفات الفعلية مباشرة قبل اعتمادها بالتقرير.

---

## 📌 ملخص الفحص

* **إجمالي الأخطاء المكتشفة (بعد إزالة التكرار):** 28
* **تصنيف الأخطاء حسب الخطورة:** حرج: 9 | متوسط: 13 | بسيط: 6

> ملاحظة مهمة: عدة أخطاء اكتشفها أكثر من عميل بشكل مستقل عن الآخر (بدون أن يرى عمل بعضهم البعض) — وهذا التطابق المستقل رفع درجة الثقة بأن الخطأ حقيقي وليس وهماً. تم دمج هذه الحالات المكررة في خطأ واحد مع الإشارة لكل العملاء الذين اكتشفوه.

---

## ✅ تحقق المشرف (Supervisor Verification)

قبل اعتماد أي خطأ بالتقرير النهائي، تم التحقق يدوياً من الأخطاء الأشد خطورة عبر فتح الملفات الفعلية وقراءتها مباشرة (وليس فقط تصديق كلام العملاء):

* تأكيد أن `vercel.json` فارغ فعلياً `{}` — أي لا يوجد أي جدولة Cron مفعّلة على الإطلاق.
* تأكيد أن فرع `discount_override` بملف الموافقات يحسب من `total_amount` الحالي وليس `pre_discount_total` — والأغرب أن الكود نفسه يحتوي تعليقاً مكتوباً من المطور يعترف بهذا الاختلاف عن الفرع المجاور `coupon_override`.
* تأكيد أن استعلام السائقين `fetchIncoming` يفلتر فقط على `status = preparing`، بينما `fetchActive` يشترط `driver_id` معروف — أي أن الطلب فعلاً "يختفي" من كل الشاشات إذا تغيّرت حالته لـ`pickup` قبل تعيين سائق.
* تأكيد أن صفحة الطلب السريع بالمحل (`admin/local/page.tsx`) تُنشئ الطلب بحالة `completed` دون أي استدعاء لدالة خصم المخزون.
* تأكيد أن سطر تحديث "وصول السائق" بصفحة التتبع لا يحتوي `await` ولا `.then()` — أي أنه فعلياً لا يُرسل للخادم إطلاقاً (تم التأكد من هذا السلوك بقراءة مكتبة Supabase نفسها).

كل هذه العينات المُتحقق منها كانت صحيحة 100%، مما يعطي ثقة عالية بباقي النتائج التي أوردها كل عميل.

---

## 👥 نتائج فحص العملاء الـ 5 (Agents Report)

### 🔴 العميل 1: أخطاء الواجهات وتفاعل المستخدم (Frontend & UI)

#### 1. عند إضافة نفس الصنف مرتين بإضافات مختلفة، تُفقد الإضافة الجديدة (سلة الزبون)
* **المكان في الكود:** `src/context/CartContext.tsx` (السطر 113-118)
* **الشرح للبسطاء:** لو الزبون ضاف صنف للسلة، ثم رجع وضاف نفس الصنف مرة ثانية لكن بإضافات مختلفة (مثلاً "جبنة زيادة")، التطبيق بيزيد العدد بس ويتجاهل الإضافة الجديدة بصمت — الزبون يحس انه طلبها لكنها ما انحفظت.
* **السبب الفني:** دالة `addItem` عند وجود صنف بنفس الـ`id` تكتفي بزيادة `quantity` وتتجاهل الـ`price`/`extras` الجديدة الممررة لها.
* **طريقة الإصلاح الموصى بها:** عند الدمج، إما تحديث السعر/الإضافات على السطر الموجود، أو معاملة الإضافات المختلفة كسطر منفصل بالسلة.
* **الخطورة:** متوسط

#### 2. إضافات الصنف (extras) تضيع من تذكرة المطبخ وقد تُحتسب مرتين
* **المكان في الكود:** `src/app/cart/page.tsx` (أسطر ~721-736, 1046-1079, 1160-1166, 1450-1453)
* **الشرح للبسطاء:** أي إضافة يختارها الزبون من نافذة تفاصيل الصنف (مثل "جبنة إضافية") قد لا تصل أبداً للمطبخ رغم دفع ثمنها، وإذا حاول الزبون يعلّم عليها مرة ثانية بشاشة المراجعة (لأنها تظهر كأنها غير مختارة)، يُحتسب سعرها مرتين.
* **السبب الفني:** الحالة `itemSelectedExtras` بصفحة السلة تبدأ فارغة دائماً ولا تُهيَّأ من `item.selected_extras_names` المحفوظة أصلاً بالسياق `CartContext`.
* **طريقة الإصلاح الموصى بها:** تهيئة `itemSelectedExtras` من `selected_extras_names` عند تحميل صفحة السلة، والتأكد من استخدام مصدر واحد للحقيقة بين العرض والحفظ النهائي.
* **الخطورة:** حرج

#### 3. نفس مشكلة الدمج بنافذة "إضافة طلب سريع" للكاشير
* **المكان في الكود:** `src/app/admin/dashboard/page.tsx` (السطر 516-522)
* **الشرح للبسطاء:** لو الكاشير أضاف نفس الصنف مرتين بإضافات مختلفة أثناء تسجيل طلب بالمحل، الإضافة الثانية تنضاع بصمت.
* **السبب الفني:** نفس نمط الخطأ رقم 1، بدالة `addToCart` بلوحة تحكم الكاشير.
* **طريقة الإصلاح الموصى بها:** نفس حل الخطأ رقم 1.
* **الخطورة:** متوسط

#### 4. حساب بلا دور معروف يعلق على شاشة تحميل دوّارة للأبد
* **المكان في الكود:** `src/components/guards/StaffGate.tsx` (السطر 13-26)، `src/context/StaffContext.tsx` (السطر 100-114)
* **الشرح للبسطاء:** أي موظف يسجّل دخول بنجاح لكن حسابه بلا دور معروف بهذا المطعم (مثلاً حساب أُلغي حديثاً)، يعلق على دائرة تحميل دوّارة للأبد بدون أي رسالة أو طريقة خروج — لازم يغلق المتصفح يدوياً.
* **السبب الفني:** `StaffGate` لا يتعامل مع حالة `ready === true && activeStaff === null` — لا يوجّه المستخدم لأي مكان.
* **طريقة الإصلاح الموصى بها:** إضافة تحويل تلقائي لصفحة `/login` مع رسالة توضيحية عند هذه الحالة.
* **الخطورة:** حرج

#### 5. نافذة الهوية البصرية (Branding) قد تعدّل إعدادات مطعم آخر بالخطأ
* **المكان في الكود:** `src/components/shared/BrandingModal.tsx` (السطر 32-50)
* **الشرح للبسطاء:** هذا الكومبوننت (غير مستخدم حالياً بأي صفحة فعلية) يجلب أول صف من جدول `restaurant_settings` بدون أي فلترة على المطعم، فلو استُخدم مستقبلاً، تعديل شعار/لون مطعم ممكن يحفظ فعلياً على مطعم آخر بالكامل.
* **السبب الفني:** الاستعلام لا يحتوي `.eq('restaurant_id', ...)`.
* **طريقة الإصلاح الموصى بها:** فلترة الاستعلام بمعرّف المطعم الحالي (نفس نمط `SettingsContext.tsx`) قبل أي إعادة استخدام لهذا الكومبوننت.
* **الخطورة:** متوسط (الكود خامل حالياً وغير مستخدم فعلياً، لكنه خطأ حقيقي بالكود ذاته)

#### 6. زر إعادة تعيين كلمة مرور الموظف بلا حماية من الضغط المزدوج
* **المكان في الكود:** `src/app/admin/settings/staff/page.tsx` (السطر 124-131, 291)
* **الشرح للبسطاء:** الزر ما ينقفل أثناء الإرسال، فضغطتين سريعتين ممكن ترسلان الطلب مرتين.
* **السبب الفني:** الزر مُعطَّل فقط بشرط طول كلمة السر وليس بحالة "جاري الإرسال".
* **طريقة الإصلاح الموصى بها:** إضافة حالة `resettingPw` وربطها بخاصية `disabled`.
* **الخطورة:** بسيط

#### 7. زر تسجيل الدخول لا يعطي أي ردة فعل عند الإرسال الفارغ
* **المكان في الكود:** `src/app/login/page.tsx` (السطر 15-16, 89-95)
* **الشرح للبسطاء:** لو المستخدم ضغط "تسجيل الدخول" بدون تعبئة الحقول، لا يظهر أي شيء — يبدو التطبيق متجمداً أو الزر معطّلاً.
* **السبب الفني:** الدالة تُرجع بصمت (`return`) بدون إظهار رسالة الخطأ الموجودة أصلاً بالكود.
* **طريقة الإصلاح الموصى بها:** إظهار رسالة الخطأ الموجودة مسبقاً عند الحقول الفارغة بدل التجاهل الصامت.
* **الخطورة:** بسيط

> **ملاحظة دمج:** خطأ "عدم وجود حماية من الضغط المزدوج على أزرار حالة الطلب" الذي رصده هذا العميل مدمج مع نتائج العميل 3 والعميل 5 — انظر البند رقم 15 بقسم "منطق العمليات" لتفاصيله الكاملة (شمل تكراراً باكتشافه من 3 عملاء مختلفين).

---

### 🟠 العميل 2: أخطاء قاعدة البيانات والـ API (Backend & Database)

#### 8. الموافقة على خصم يُحسب من السعر بعد خصم سابق بدل السعر الأصلي
* **المكان في الكود:** `src/app/api/approvals/[id]/resolve/route.ts` (السطر 115-119)
* **الشرح للبسطاء:** لو طُبّق خصم على طلب مسبقاً، ثم وافق المالك لاحقاً على طلب خصم إضافي كان معلّقاً، النظام يطبّق النسبة الموافَق عليها على السعر بعد الخصم الأول — فيصير الخصم الفعلي أكبر بكثير مما وافق عليه المالك، أي خسارة إضافية غير مقصودة بسعر البيع.
* **السبب الفني:** فرع `discount_override` يحسب `originalTotal` من `order.total_amount` الحالي بدل `order.pre_discount_total ?? order.total_amount`، بخلاف الفرع المجاور `coupon_override` بنفس الملف الذي يستخدم الطريقة الصحيحة، ولا يحفظ `pre_discount_total` أصلاً. **(تم التحقق مباشرة من الكود وتأكيد المشكلة، وهي مكتشفة أيضاً بشكل مستقل من العميل 3).**
* **طريقة الإصلاح الموصى بها:** جعل فرع `discount_override` يحسب من `pre_discount_total ?? total_amount` ويحفظ `pre_discount_total`، بنفس منطق `coupon_override`.
* **الخطورة:** حرج

#### 9. إلغاء إيقاف المطعم لا يعيد فتحه فعلياً للزبائن
* **المكان في الكود:** `src/app/api/super-admin/restaurants/route.ts` (السطر 338)
* **الشرح للبسطاء:** لما السوبر أدمن يلغي إيقاف مطعم موقوف، المطعم يبقى مخفياً عن الزبائن ولا يظهر بالتطبيق حتى يلاحظ المالك بنفسه ويعيد فتحه يدوياً من الإعدادات.
* **السبب الفني:** `suspend:true` يجبر `is_closed:true` بشكل صحيح، لكن `suspend:false` لا يعيد ضبط `is_closed` إلى `false`.
* **طريقة الإصلاح الموصى بها:** عند `suspend:false`، إعادة `is_closed` إلى `false` أيضاً.
* **الخطورة:** متوسط

#### 10. إنشاء مطعم جديد قد يفشل بصمت بدون أي تنبيه
* **المكان في الكود:** `src/app/api/super-admin/restaurants/route.ts` (السطر 53-73، دالة `seedNewRestaurant`)
* **الشرح للبسطاء:** ممكن يُنشأ مطعم جديد وتظهر رسالة نجاح للسوبر أدمن، لكن بالخلفية يفشل إنشاء "إعدادات المطعم" أو "فئات المخزون الافتراضية" بصمت — فيفتح المالك حسابه ويجد صفحة الإعدادات أو المخزون فارغة دون أي تفسير. **(مكتشف أيضاً بشكل مستقل من العميل 4).**
* **السبب الفني:** إدراج `restaurant_settings` و`inventory_categories` لا يتحقق من قيمة `error` المرجعة من Supabase.
* **طريقة الإصلاح الموصى بها:** التحقق من `error` على كل إدراج، وإرجاع خطأ واضح للسوبر أدمن إن فشل أي منهما.
* **الخطورة:** متوسط

---

### 🟡 العميل 3: أخطاء منطق النظام الخاص بالمشروع والعمليات (Business Logic)

#### 11. الطلب قد يعلق للأبد بلا سائق ولا يقدر أي أحد يستلمه
* **المكان في الكود:** `src/app/admin/dashboard/page.tsx` (زر "جاهز للتسليم"، السطر ~1448)، `src/app/driver/dashboard/page.tsx` (دالة `fetchIncoming`، السطر 110-124)
* **الشرح للبسطاء:** إذا ضغط الموظف زر "جاهز للتسليم" قبل أن يقبل أي سائق الطلب، يختفي الطلب من تطبيق كل السائقين نهائياً ولا يقدر أي سائق يستلمه بعدها — الطلب يبقى عالقاً بلا توصيل والزبون لن يستلم طلبه أبداً إلا بتدخل يدوي مباشر بقاعدة البيانات. **(تم التحقق مباشرة من الكود وتأكيد المشكلة).**
* **السبب الفني:** زر "جاهز للتسليم" ينقل الحالة من `preparing` إلى `pickup` بدون التحقق من وجود `driver_id`، بينما استعلام السائقين لاكتشاف الطلبات المتاحة يفلتر فقط `status = preparing`، ولا توجد واجهة إدارية لتعيين سائق يدوياً.
* **طريقة الإصلاح الموصى بها:** تعطيل/إخفاء زر "جاهز للتسليم" ما دام `driver_id` فارغاً، و/أو جعل استعلام السائقين يشمل طلبات `pickup` غير المعيَّنة أيضاً.
* **الخطورة:** حرج

#### 12. المخزون المخصوم لا يُعاد أبداً عند إلغاء/استرجاع طلب بعد بدء التجهيز
* **المكان في الكود:** `src/app/admin/dashboard/page.tsx` (دالة `deductStockForOrder`، السطر 42-67)، `src/app/api/orders/[id]/void/route.ts`، `src/app/api/orders/[id]/refund/route.ts`
* **الشرح للبسطاء:** إذا أُلغي طلب بعد ما بدأ تجهيزه (يعني المكونات انخصمت من المخزون فعلياً)، المخزون ما يرجع أبداً حتى لو الأكل ما طلع للزبون — يتراكم فرق وهمي بين المخزون المسجّل والمخزون الحقيقي، وقد يمنع بيع أصناف موجودة فعلاً بسبب تنبيهات نفاد مخزون كاذبة.
* **السبب الفني:** لا يوجد أي كود بالمشروع بأكمله يُدرج حركة مخزون معاكسة (`RETURN`/`IN`) عند الإلغاء/الاسترجاع بعد خصم المخزون.
* **طريقة الإصلاح الموصى بها:** عند الإلغاء/الاسترجاع لطلب سبق خصم مخزونه، إدراج حركة إعادة مخزون معاكسة بنفس كميات الوصفة.
* **الخطورة:** حرج

#### 13. طلبات الكاشير المباشرة (POS بالمحل) لا تخصم من المخزون إطلاقاً
* **المكان في الكود:** `src/app/admin/local/page.tsx` (دالة `submitOrder`، السطر 135-173)
* **الشرح للبسطاء:** أي طلب يُباع من الكاشير مباشرة بالمحل لا يخصم أي مكوّن من المخزون إطلاقاً — المخزون المعروض بالنظام يبقى أعلى من الواقع بشكل دائم ومتراكم مع كل عملية بيع كاشير. **(تم التحقق مباشرة من الكود وتأكيد غياب استدعاء دالة خصم المخزون).**
* **السبب الفني:** الطلب يُنشأ مباشرة بحالة `completed` دون استدعاء `deductStockForOrder`، بخلاف مسار الطلب السريع المشابه بلوحة التحكم الرئيسية الذي يستدعيها بشكل صحيح.
* **طريقة الإصلاح الموصى بها:** استدعاء نفس دالة خصم المخزون عند إنشاء طلب POS محلي، تماماً كما هو مطبّق بالفعل بمسار الطلب السريع الآخر.
* **الخطورة:** حرج

#### 14. تنفيذ طلب الموافقة لا يتحقق من حالة الطلب الحالية قبل التطبيق
* **المكان في الكود:** `src/app/api/approvals/[id]/resolve/route.ts` (السطر 81-174)
* **الشرح للبسطاء:** إذا انتظر طلب موافقة (خصم/إلغاء/استرجاع) فترة، وتغيّرت حالة الطلب بطريقة أخرى بهذه الأثناء، الموافقة على الطلب القديم لاحقاً ممكن ترجع حالة الطلب لحالة متناقضة (مثلاً "ملغى" بعد ما كان "مسترجع") أو تطبّق خصماً على طلب أصلاً ملغى.
* **السبب الفني:** لا يوجد تحقق من `order.status` الحالي قبل تنفيذ الإجراء الموافَق عليه، بخلاف مسارات التطبيق المباشر (void/refund/discount) التي ترفض العمل على طلب موقوف/مسترجع مسبقاً.
* **طريقة الإصلاح الموصى بها:** إعادة جلب حالة الطلب والتحقق منها مباشرة قبل تطبيق الإجراء الموافَق عليه.
* **الخطورة:** متوسط

#### 15. الضغط المزدوج السريع على أزرار حالة الطلب يسبب خصم مخزون مضاعف وإشعارات مكررة
* **المكان في الكود:** `src/app/admin/dashboard/page.tsx` (دالة `handleAction`، السطر 929-973 وأزرارها بلا حماية بالأسطر ~1270, 1443, 1640, 1785, 1939)
* **الشرح للبسطاء:** الضغط مرتين بسرعة على زر "ابدأ التجهيز" لنفس الطلب (شيء وارد جداً على شاشة لمس بمطبخ مزدحم أو بطء إنترنت) ممكن يخصم مكونات الوصفة من المخزون **مرتين** لطلب واحد فقط، وقد يرسل إشعار مكرر للسائق أو الزبون.
* **السبب الفني:** لا يوجد حالة "قيد المعالجة" مرتبطة بالطلب لتعطيل الزر أثناء الطلب الأول، وبما أن التحديث غير متزامن (async)، الضغطة الثانية تصل قبل تحديث الحالة برة الشاشة فتتكرر نفس المنطق (خصم المخزون + إرسال الإشعار). **(هذا الخطأ اكتُشف بشكل مستقل من 3 عملاء مختلفين: العميل 1، العميل 3، والعميل 5 — تطابق مستقل يرفع الثقة بوجوده فعلياً).**
* **طريقة الإصلاح الموصى بها:** تتبع حالة `processingOrderId` وتعطيل الزر لنفس الطلب أثناء المعالجة (بنفس نمط الحماية الموجود أصلاً بتطبيق السائق).
* **الخطورة:** متوسط

#### 16. إغلاق الوردية يحسب "الكاش المتوقع" مرتين عند وجود أكثر من كاشير بنفس الوقت
* **المكان في الكود:** `src/app/api/shifts/[id]/close/route.ts` (السطر 59-70)، `src/app/api/shifts/open/route.ts` (السطر 30-39)
* **الشرح للبسطاء:** إذا كان أكثر من كاشير شغّال بنفس الوقت (ورديات متداخلة زمنياً)، إغلاق وردية أي كاشير يحسب كل طلبات المطعم المكتملة بهذه الفترة — حتى الطلبات التي نفّذها الكاشير الآخر — ضمن "المبلغ المتوقع" لكل واحد منهم، مما يعطي تقرير إقفال وتنبيهات فرق كاش غير دقيقة.
* **السبب الفني:** منع الوردية المزدوجة يقتصر على نفس الموظف فقط، وحساب المبلغ المتوقع يجمع كل الطلبات المكتملة بالفترة الزمنية بدون ربطها بكاشير/وردية محددة.
* **طريقة الإصلاح الموصى بها:** منع تداخل الورديات المفتوحة بنفس المطعم، أو إضافة عمود ربط طلب↔كاشير لحساب دقيق لكل وردية.
* **الخطورة:** متوسط

---

### 🔵 العميل 4: أخطاء الربط بالخدمات الخارجية (Integrations)

#### 17. إشعارات الزبون مبنية بالكامل لكنها لا تُرسل أبداً
* **المكان في الكود:** `src/app/api/push/notify-customer/route.ts` (الملف كاملاً غير مستخدم)، `src/app/track/page.tsx` (السطر 608-631)
* **الشرح للبسطاء:** الزبون يفعّل الإشعارات من صفحة تتبع الطلب ويوافق عليها، لكنه لن يستلم أي إشعار أبداً عند تغيّر حالة طلبه (قُبل، بالطريق، وصل) — الميزة موجودة بالواجهة لكنها معطّلة فعلياً بالخلفية لأنه لا يوجد أي كود بالمشروع يستدعي هذا المسار.
* **السبب الفني:** لا يوجد أي استدعاء لمسار `/api/push/notify-customer` من أي نقطة تغيّر حالة طلب بالكود بأكمله (تم التأكد بالبحث بكامل مجلد `src/`).
* **طريقة الإصلاح الموصى بها:** استدعاء هذا المسار من كل نقطة تغيّر فعلياً حالة الطلب (قبول السائق، التسليم، الرفض...).
* **الخطورة:** حرج

#### 18. مهمة إلغاء الطلبات المنتهية لا تعمل أبداً تلقائياً — لا توجد أي جدولة مفعّلة
* **المكان في الكود:** `vercel.json` (المحتوى بالكامل `{}`)، `src/app/api/cron/expire-orders/route.ts`
* **الشرح للبسطاء:** الطلبات المعلّقة التي ما حد قبلها (بلا سائق) يفترض أن تُلغى تلقائياً بعد 5 دقائق، لكن هذا لن يحدث أبداً تلقائياً لأنه لا توجد أي جدولة تشغّل هذا الإجراء دورياً — تبقى الطلبات معلّقة للأبد إلا لو استدعى أحد الرابط يدوياً. **(تم التحقق مباشرة وتأكيد أن ملف الجدولة فارغ فعلياً).**
* **السبب الفني:** ملف `vercel.json` لا يحتوي مصفوفة `crons` تستدعي هذا المسار، ولا يوجد أي بديل خارجي (لا GitHub Action ولا خدمة جدولة أخرى) يستدعيه.
* **طريقة الإصلاح الموصى بها:** إضافة تعريف Cron بملف `vercel.json` يستدعي هذا المسار كل 15 دقيقة مثلاً.
* **الخطورة:** حرج

#### 19. غياب متغيرات بيئة الإشعارات يعطّل 3 مسارات إشعارات بالكامل بخطأ غير معالج
* **المكان في الكود:** `src/app/api/push/notify/route.ts`، `src/app/api/push/broadcast/route.ts`، `src/app/api/push/notify-customer/route.ts` (السطر ~14-19 بكل منها)
* **الشرح للبسطاء:** لو انحذف أو انسحب أحد متغيرات إعدادات الإشعارات بالخطأ (مثلاً عند نشر بيئة جديدة)، كل إشعارات push للسائقين تتوقف فوراً بخطأ فني بدل تجاهل الإشعار بهدوء، كما هو مطبّق فعلياً بمسار مشابه آخر بنفس المشروع.
* **السبب الفني:** استدعاء `webpush.setVapidDetails` يُنفَّذ مباشرة بدون تحقق مسبق من وجود المتغيرات الثلاثة، وهذه الدالة تنفجر (throw) فوراً إذا كانت القيم فارغة.
* **طريقة الإصلاح الموصى بها:** تطبيق نفس نمط التحقق الآمن الموجود بملف `notify-owner-push.ts` على الثلاث مسارات.
* **الخطورة:** متوسط

#### 20. رسالة "تمت الموافقة" تظهر للزبون قبل إنشاء حساب مطعمه فعلياً
* **المكان في الكود:** `src/app/api/super-admin/signup-requests/route.ts` (السطر 50-56)، `src/app/track-signup/page.tsx` (السطر 208-231)
* **الشرح للبسطاء:** الزبون يشوف رسالة "تمت الموافقة، سجّل دخولك الآن" بينما حسابه الفعلي (سجل المطعم) لسا ما انربط من طرف السوبر أدمن (خطوة ثانية منفصلة يدوية) — فيحاول تسجيل الدخول وتفشل المحاولة برسالة غامضة، رغم أن لا شيء خطأ من طرفه.
* **السبب الفني:** الموافقة تتم على خطوتين منفصلتين يدوياً (تحديث الحالة، ثم إنشاء المطعم فعلياً)، لكن صفحة تتبع طلب التسجيل تعرض رسالة الجاهزية فور اكتمال الخطوة الأولى فقط.
* **طريقة الإصلاح الموصى بها:** عدم إظهار شاشة "جاهز، سجّل دخولك" إلا إذا اكتمل ربط المطعم فعلياً، وإلا عرض حالة وسيطة "تمت الموافقة، جاري تجهيز حسابك".
* **الخطورة:** متوسط

#### 21. عملية ربط المطعم الجديد غير آمنة عند التكرار — قد ينتج مطعم مكرر
* **المكان في الكود:** `src/app/api/super-admin/restaurants/route.ts` (السطر 172-175)
* **الشرح للبسطاء:** بحالة نادرة (فشل مؤقت بقاعدة البيانات لحظة إتمام الربط)، لو أعاد السوبر أدمن نفس عملية "ربط المطعم" ظناً منه أنها فشلت، ممكن يُنشأ مطعم مكرر لنفس صاحب الحساب، وقد يتغيّر إيميل دخوله الداخلي مرة أخرى بشكل غير متوقع.
* **السبب الفني:** تحديث `linked_restaurant_id` (وهو ما يمنع التكرار مستقبلاً) لا يتحقق من نجاحه، فلو فشل هذا التحديث تحديداً بعد نجاح إنشاء المطعم، الحارس من التكرار لن يعمل بالمحاولة التالية.
* **طريقة الإصلاح الموصى بها:** التحقق من نجاح هذا التحديث وإرجاع خطأ صريح عند الفشل بدل الاستمرار بصمت.
* **الخطورة:** متوسط

#### 22. تضارب بمعالجة اشتراكات الإشعارات المنتهية (410 مقابل 404)
* **المكان في الكود:** `src/app/api/push/notify/route.ts`، `src/app/api/push/broadcast/route.ts`، `src/lib/api/notify-owner-push.ts`
* **الشرح للبسطاء:** لو انتهت صلاحية اشتراك إشعارات سائق أو مالك بطريقة يرجع فيها خطأ مختلف (404 بدل 410)، النظام لن ينظّف هذا الاشتراك الميت أبداً، وسيستمر بمحاولة إرسال إشعارات فاشلة له مع كل طلب جديد.
* **السبب الفني:** هذه المسارات الثلاثة تتحقق فقط من كود الخطأ 410، بينما مسار مشابه آخر بنفس المشروع (إشعارات الزبون) يتحقق من 410 و404 معاً.
* **طريقة الإصلاح الموصى بها:** توحيد الشرط ليشمل 410 و404 بكل المسارات الأربعة.
* **الخطورة:** بسيط

---

### 🟣 العميل 5: أخطاء الأداء وتجمد التطبيق (Performance & Race Conditions)

#### 23. تحديث "وصول السائق" لا يُرسل لقاعدة البيانات إطلاقاً
* **المكان في الكود:** `src/app/delivery/[orderId]/page.tsx` (السطر 377)
* **الشرح للبسطاء:** عند اقتراب السائق من الزبون، من المفترض أن تظهر للزبون رسالة "السائق وصل!" أثناء التتبع، لكن هذا لا يحدث أبداً لأن استدعاء حفظ الحالة بقاعدة البيانات ناقص — الميزة معطّلة بصمت بالكامل لكل طلب توصيل. **(تم التحقق مباشرة من الكود وتأكيد غياب `await`/`.then()`، وأن هذا يعني عملياً عدم إرسال أي طلب للخادم إطلاقاً).**
* **السبب الفني:** استعلام Supabase بهذا السطر لا يحتوي `await` ولا `.then()` — واستعلامات Supabase لا تُنفَّذ فعلياً إلا عند انتظارها، فالسطر بلا تأثير فعلي إطلاقاً.
* **طريقة الإصلاح الموصى بها:** إضافة `await` (بجعل الدالة المحيطة async) أو `.then()` مع معالجة الخطأ.
* **الخطورة:** حرج

#### 24. مؤقّت زمني ميت يُترك شغّالاً بدون داعٍ بتطبيق السائق
* **المكان في الكود:** `src/app/driver/dashboard/page.tsx` (السطر 78)
* **الشرح للبسطاء:** كل مرة يفتح السائق صفحة الطلبات، يبقى مؤقّت شغّال بالخلفية للأبد بدون أي فائدة فعلية — استهلاك غير ضروري لموارد الجهاز. **(اكتُشف بشكل مستقل من عميلين مختلفين: العميل 1 والعميل 5).**
* **السبب الفني:** `setInterval` بمعالج فارغ، بدون حفظ معرّفه ولا تنظيفه عند إلغاء تحميل المكوّن.
* **طريقة الإصلاح الموصى بها:** حذف هذا السطر بالكامل — لا وظيفة فعلية له حالياً.
* **الخطورة:** بسيط

#### 25. اشتراكات البث اللحظي غير مفلترة — كل مطعم "يسمع" تحديثات كل المطاعم الأخرى
* **المكان في الكود:** `src/app/admin/dashboard/page.tsx` (قناة `dash-live`)، `src/app/admin/drivers/page.tsx`، `src/app/admin/archive/page.tsx`، `src/app/driver/dashboard/page.tsx`، `src/app/home/HomeClient.tsx` (قائمة الطعام العامة للزبائن)
* **الشرح للبسطاء:** أي تعديل بسيط بأي مطعم آخر على المنصة يجعل تطبيقك يعيد تحميل البيانات من جديد. مع زيادة عدد المطاعم، ستصبح كل الشاشات (الطلبات، السائقين، الأرشيف، وحتى قائمة الطعام التي يتصفحها الزبائن) أبطأ تدريجياً وتستهلك بيانات أكثر بدون داعٍ.
* **السبب الفني:** اشتراكات `postgres_changes` بهذه الملفات لا تحتوي `filter: restaurant_id=eq....`، بخلاف اشتراكات أخرى صحيحة بنفس المشروع (`SettingsContext`, `LowStockAlert`, إلخ).
* **طريقة الإصلاح الموصى بها:** إضافة فلتر معرّف المطعم لكل اشتراك بث لحظي بهذه الملفات.
* **الخطورة:** متوسط (وتتجه لحرج بحالة `HomeClient.tsx` تحديداً مع نمو عدد الزوار)

#### 26. تحديثات متعددة غير متزامنة قد تُظهر حالة قديمة للطلب لحظياً
* **المكان في الكود:** `src/app/track/page.tsx` (البث اللحظي، الاستطلاع الدوري كل 3 ثواني، وإعادة الجلب عند فتح التبويب)
* **الشرح للبسطاء:** أثناء تتبع الطلب، قد تشاهد حالة الطلب "ترتد" لحظياً لحالة قديمة قبل أن تصحّح نفسها خلال ثوانٍ قليلة، بسبب تعدد مصادر التحديث دون أي تنسيق بينها لمعرفة أيها الأحدث.
* **السبب الفني:** لا يوجد رقم تسلسلي أو آلية إلغاء (AbortController) لتفادي أن يستبدل رد قديم رداً أحدث منه بنفس الحالة.
* **طريقة الإصلاح الموصى بها:** استخدام عدّاد طلبات متزايد يُقارَن قبل تحديث الحالة، أو الاكتفاء بآلية تحديث واحدة (البث اللحظي فقط لإعادة التحقق بدل 3 مصادر مستقلة).
* **الخطورة:** متوسط

#### 27. تسريب ذاكرة بسيط لملف صوت التنبيه
* **المكان في الكود:** `src/app/admin/layout.tsx` (السطر 12-24, 31-38)
* **الشرح للبسطاء:** كل مرة يُعاد تحميل واجهة لوحة تحكم المطعم بالكامل، يبقى ملف صوت التنبيه محجوزاً بذاكرة المتصفح للأبد بدون تحرير.
* **السبب الفني:** `URL.createObjectURL` لا يُحرَّر أبداً عبر `URL.revokeObjectURL` بدالة التنظيف.
* **طريقة الإصلاح الموصى بها:** حفظ الرابط بمرجع (ref) وتحريره بدالة تنظيف الـ`useEffect`.
* **الخطورة:** بسيط

#### 28. إعادة حساب غير ضرورية لمجموع الإيرادات بصفحة الإحصائيات
* **المكان في الكود:** `src/app/admin/statistics/page.tsx` (السطر 536)
* **الشرح للبسطاء:** صفحة الإحصائيات تعيد جمع كل إيرادات الطلبات من الصفر لكل قسم (تصنيف) في كل مرة تُعاد فيها رسمة الشاشة (حتى مجرد الكتابة بمربع البحث)، بدل استخدام رقم محسوب مسبقاً — مع نمو عدد الطلبات قد تصبح هذه الصفحة أبطأ ملحوظاً.
* **السبب الفني:** استدعاء `orders.reduce(...)` يتكرر داخل `.map()` بدل حساب المجموع مرة واحدة بـ`useMemo`.
* **طريقة الإصلاح الموصى بها:** نقل حساب المجموع خارج `.map()` إلى `useMemo` يُحسب مرة واحدة فقط.
* **الخطورة:** بسيط

---

## 🚀 خطة العمل والإصلاح (Action Plan)

### المرحلة 1 — أخطاء حرجة (أثر مباشر على الطلبات/المخزون/الإيرادات، يُنصح بالإصلاح فوراً)
1. **#13** — طلبات الكاشير المحلي لا تخصم مخزون إطلاقاً (فرق مخزون متراكم يومياً)
2. **#12** — لا استرجاع للمخزون عند إلغاء طلب بعد بدء التجهيز
3. **#11** — الطلب يعلق للأبد بلا سائق إذا انتقل لحالة "جاهز للتسليم" قبل تعيين سائق
4. **#8** — حساب خصم الموافقة من سعر خاطئ (خسارة مالية مباشرة)
5. **#2** — إضافات الصنف تضيع من تذكرة المطبخ أو تُحتسب مرتين
6. **#18** — لا توجد جدولة Cron فعّالة إطلاقاً (الطلبات المعلّقة لا تُلغى تلقائياً)
7. **#17** — إشعارات الزبون معطّلة بالكامل رغم وجود واجهتها
8. **#23** — تحديث "وصول السائق" لا يصل للخادم إطلاقاً
9. **#4** — حساب بلا دور يعلق على شاشة تحميل بلا مخرج

### المرحلة 2 — أخطاء متوسطة (تُصلَح خلال الأسابيع القادمة)
#1, #3, #5, #9, #10, #14, #15, #16, #19, #20, #21, #25, #26

### المرحلة 3 — أخطاء بسيطة (تحسينات تراكمية، يمكن جدولتها بسهولة)
#6, #7, #22, #24, #27, #28

---

## 🧭 ملاحظة ختامية

هذا التقرير يغطي **الأخطاء البرمجية المنطقية والتشغيلية فقط** كما طُلب صراحة — لم يتم تضمين أي ملاحظات أمنية (صلاحيات، RLS، تسريب بيانات) رغم ملاحظة العملاء لبعض الحالات ذات الطابع الأمني أثناء الفحص؛ تلك الحالات استُبعدت عمداً من هذا التقرير وتُترك للفحص الأمني المنفصل القادم.
